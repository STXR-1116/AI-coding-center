# 微任务 P1-2d：CreateTaskDialog 接 API（需求下拉 + createTask mutation）

背景：P1-5a 已实现 GET /api/v1/requirements（列表+详情）。本任务：CreateTaskDialog（src/components/CreateTaskDialog.tsx）从 AppContext mock（addTask）迁移到 REST：需求下拉（真实需求）+ createTask mutation。

## 范围（严格）

### 1. 数据层（src/api/requirements.ts + src/queries/requirements.ts 新文件）
- `listRequirements(params?: { status?: string })` → `{ data: RequirementDto[], page }`（unwrap:false——与 tasks 一致）
- RequirementDto：`id/title/description/status/priority/submitterId/submitterType/createdAt/updatedAt`
- `useRequirements()`：useQuery(["requirements"]) + `handleApiError` 复用（从 queries/tasks.ts 导出或复制——**优先复用**：把 handleApiError 从 queries/tasks.ts 移到 src/queries/errors.ts 导出，tasks.ts re-export 保持兼容）

### 2. CreateTaskDialog（src/components/CreateTaskDialog.tsx）
- `const { data: reqsData } = useRequirements()` → 需求下拉（select）：显示需求 title，值 requirementId
- 移除 `useApp().addTask` + `useApp().projects/activeProjectId`（projectId 不再传给后端——后端 createTask 无 projectId）
- `const createMutation = useCreateTask()`
- submit：
  - 校验：title 必填 + **requirementId 必选**（下拉默认选第一个需求；需求加载中禁用提交）
  - `createMutation.mutateAsync({ requirementId, title, spec: summary })` → 成功 notify(`任务已创建：{id}` title '任务已创建') + onCreated(id) + onClose
  - 失败 notify(handleApiError(e), { tone: 'error' })
- 表单字段：title（必填）/ summary（→ spec）/ requirement 下拉（必选）/ priority + executionMode **移除**（后端 MVP 不支持——诚实：去掉字段，或保留 disabled？——**移除**，避免假操作）

### 3. 验证
- `npm run build` + `npm test`（App.test.tsx mock useRequirements 返回 2 条需求；现有"新建任务"相关测试若挂调整——检查 App.test.tsx 是否有 create 测试）
- 浏览器实测：
  1. 点"新建任务" → 对话框：需求下拉（真实需求）+ title/summary
  2. 填写 → 创建 → POST /tasks → 成功 toast + 对话框关闭 + 任务列表刷新出现新任务（invalidateQueries）
  3. 错误路径：空 title → 前端校验拦截（不提交）

## 不做
- priority/executionMode 字段（后端 PATCH 未支持——等 P1 后期）
- 需求创建入口（P1-5b 后端 + 前端需求页 P1-5d）

## 验收
1. build + test 全绿
2. 浏览器实测 3 项通过（真实创建任务落库）
3. git commit + push

## 审查（流程铁律）
完成后调用 codex-review-and-fix skill 审查（手动模式）；报告 高/中/低 + 修复建议写 codex-review-p1-2d.md；修复后重新 build+test。
