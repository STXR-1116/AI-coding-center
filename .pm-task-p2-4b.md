# 微任务 P2-4b：AnalyticsPage 真实化 + 新增 UsersPage（P2 收官）

背景：P2-4a 已实现 dashboard/summary + metrics/summary + users + audit-logs REST。本任务：AnalyticsPage（/analytics 可观测中心，当前 useApp mock）接真实统计 + 新增 UsersPage（用户管理——README 有接口前端无页面）。

## 范围（严格）

### 1. 数据层
- **src/api/dashboard.ts**：fetchDashboardSummary()（GET /dashboard/summary）/ fetchMetricsSummary(params?)（GET /metrics/summary）/ fetchAuditLogs(params?)（GET /audit-logs，unwrap:false 分页）
- **src/queries/dashboard.ts**：dashboardKeys + useDashboardSummary + useMetricsSummary(range?) + useAuditLogs(filters?)
- **src/api/users.ts + src/queries/users.ts**：listUsers/fetchUsers + updateUser(id, patch) + useUsers + useUpdateUser（onSuccess invalidate lists）——参照 agents 模式
- **src/types.ts**：DashboardSummaryDto/MetricsPointDto/AuditLogDto/UserDto（按 P2-4a 三个 dto 实际字段）

### 2. AnalyticsPage 真实化
- useApp().tasks/projects mock 统计 → useDashboardSummary（任务状态分布 tasksByStatus/Agent 状态 agentsByStatus/token 总量——**看页面现有图表结构**：Recharts 已装（v3）——饼图/柱状图数据源换真实）
- range 切换（30d/7d/90d）→ useMetricsSummary(range)（时间序列）
- 保留 UI 结构（卡片/图表布局）——只换数据源 + loading/error/空态
- agents mock 引用移除

### 3. 新增 UsersPage（src/pages/UsersPage.tsx）
- 路由 /users + AppShell nav（模块 gate moduleId——看 AppShell 的 nav 结构——加"用户管理"项——**注意**：moduleId 用现有可用的（如 'account'？——看 moduleSettings 有无 account——没有则不加 gate 或直接用 RequireAuth）
- 页面：用户列表（真实 4 用户：用户名/角色/状态）+ 角色切换（下拉：LEADER/ADMIN/PM/EMPLOYEE → useUpdateUser PATCH） + 状态切换（启用/停用）
- **自改保护**：当前用户行禁用角色/状态操作（tooltip"不能修改自己"）
- 审计日志区（可选）：下方列表 useAuditLogs（最近 10 条：action/actor/at）
- 参照 AgentsPage 布局（resource-pages.css 复用）

### 4. 验证
- npm run build + npm test（App.test 若挂 → vi.mock queries/dashboard + queries/users——**注意**：mock 值用模块级稳定引用（P2-3b 教训：内联对象触发无限重渲染））
- 浏览器实测：
  1. /analytics → 真实统计（任务状态分布数字与 tasks 页一致）
  2. /users → 4 真实用户 + 角色下拉 + 状态切换（改 employee 角色再改回——**别动 leader/admin 自己**）
  3. audit 日志区真实
- git commit + push

## 不做
- 用户创建/删除（后端无）
- 审计详情页
- 通知页

## 审查（流程铁律）
完成后调用 codex-review-and-fix skill 审查（手动模式）；报告写 codex-review-p2-4b.md；修复后重新 build+test。

**开工前先用 mcp__codegraph__codegraph_explore 探索**（查询 "AnalyticsPage tasks projects agents mock Recharts; AppShell nav moduleId; dashboard-dto user-dto audit-dto 字段" 等，项目根 C:\Users\13588\Desktop\AI_Coding_Center）。
