# 微任务 P3-4b：SettingsPage 模块开关接 REST

背景：P3-4a 已实现 GET /v1/modules + PATCH /v1/modules/{key}。本任务：SettingsPage 模块开关从 AppContext mock（moduleSettings/toggleModule）迁移到真实 REST——P3"多页状态收敛到 query 缓存"收尾项。

## 范围（严格）

### 1. 数据层
- **src/api/modules.ts**：listModules()（GET /modules——unwrap:false？——看返回形状：数组或 {data:[...]}——按主工程 module-dto 实际）/ setModuleToggle(key, enabled)（PATCH）
- **src/queries/modules.ts**：modulesKeys + useModules + useSetModuleToggle（onSuccess invalidate modules + **同步 AppContext？——决定**：SettingsPage 独立用 useModules（不再依赖 AppContext moduleSettings）——AppContext 的 moduleSettings/toggleModule 保留（其他消费方？——grep：ModuleGate 用 useApp().moduleSettings？——若 ModuleGate 也读 AppContext 则模块禁用路由不实时——**MVP**：SettingsPage 用 REST 真实化 + ModuleGate 保持现状（下次刷新生效）——标注）
- **src/types.ts**：ModuleToggleDto（id/key/enabled/risk?/label?——按实际）

### 2. SettingsPage 迁移
- moduleSettings（useApp）→ useModules()（loading/error 态 + 真实 7 个模块开关）
- toggleModule mock → useSetModuleToggle().mutate({ key, enabled: !current }) → onSuccess invalidate + toast（模块变更即时生效——**注意**：切换 task_dispatch 会真影响调度——UI 有确认弹窗（现有 pendingModule 逻辑保留））
- 保留：搜索/风险筛选（前端过滤）/pendingModule 二次确认

### 3. 验证
- npm run build + npm test（App.test 若挂 → vi.mock queries/modules——模块级稳定引用！）
- 浏览器实测：/settings 模块开关 → 真实 7 模块（任务分发/Agent 配置/版本库/知识库/技能管理/账号权限/数据看板）→ 切换一个（如"技能管理"→ 关 → 开——验证后还原）→ toast + 状态变化
- git commit + push

## 不做
- ModuleGate 实时联动（路由守卫下次刷新生效——标注）
- 平台参数 tab（defaultConfig mock——后续）
- 通知策略 tab

## 审查（流程铁律）
完成后调用 codex-review-and-fix skill 审查（手动模式）；报告写 codex-review-p3-4b.md；修复后重新 build+test。

**开工前先用 mcp__codegraph__codegraph_explore 探索**（查询 "SettingsPage moduleSettings toggleModule pendingModule ModuleGate; useApp app-context 模块; module-toggle ToggleItem" 等，项目根 C:\Users\13588\Desktop\AI_Coding_Center）。
