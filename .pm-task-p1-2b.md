# 微任务 P1-2b：TasksPage 数据源迁移（mock → React Query + REST，读部分）

背景：P1-2a 已完成数据层（api/tasks.ts + queries/tasks.ts）。本任务把 TasksPage（src/pages/TasksPage.tsx，473 行）的**读路径**从 AppContext mock 迁移到 REST。写操作（inspector 按钮/创建对话框）下一微任务（P1-2c）。

## 范围（严格——只动读路径）

### 1. 数据源替换（TasksPage 主组件，line 371 起）
- `const { tasks } = useApp()` → `const { data: tasksData, isPending } = useTasks({ status: status === 'all' ? undefined : status, q: query.trim() || undefined, limit: 100 })`
- `const tasks = tasksData?.data ?? []`（保持下游兼容——任务看板/列表/时间线视图不用改）
- `selectedId` 默认值：`tasks[0]?.id`（保持）——数据加载后默认选第一个
- `scope`（all/mine/assigned）→ 前端过滤：
  - `all`：全部
  - `assigned`：assignee 非空
  - `mine`：assignee === 当前用户（useApp().user?.name —— AppContext auth.user.name）
  - 过滤在 useMemo 里叠加（数据 + scope + sortNewest）
- `sortNewest`：按 updatedAt 排序（REST 返回已按 createdAt desc——前端按 updatedAt 再排）
- isPending → 加载态（保持现有点击占位行为即可，不新增 UI 骨架——最小改动）

### 2. 筛选器接线
- `status` select → useTasks 的 status 参数（**后端过滤**，删前端过滤分支）
- `query` 搜索框 → useTasks 的 q 参数（**后端搜索**，输入防抖 300ms：useState + useEffect setTimeout）
- 注意：status/q 变化触发 query 重拉——useTasks 依赖 `[status, debouncedQuery]`（React Query 自动）

### 3. TaskInspector 数据
- inspector 显示的任务：从 tasks 数组找（selectedId）——不改（已由父组件传 task）
- 读路径不动：进度/徽章/分配人/上下文等展示字段 TaskDto 已有（progress/tokenBudget/contextUsage/assignee）

### 4. 清理
- TasksPage 不再依赖 `useApp().tasks`（保留 useApp() 其他用途如 user）
- 若 AppContext 的 tasks 状态被其他页面使用则保留（本任务只改 TasksPage）

### 5. 验证
- `npm run build` + `npm test`（22 个不回归——App.test.tsx 若渲染 TasksPage 需 mock useTasks 或真实 fetch——**注意**：App.test.tsx 现有测试可能因 TasksPage 发请求失败而挂——若挂：vi.mock('../queries/tasks', () => ({ useTasks: () => ({ data: { data: [], page: { nextCursor: null, hasMore: false } }, isPending: false }) }))）
- 浏览器实测（dev 4173 + 主工程 3003）：
  1. 登录 leader → /tasks → **真实任务列表**（主工程 DB 任务，非 mock——检查任务标题是否与 DB 一致）
  2. 状态筛选切换 → 列表变化
  3. 搜索 → 列表过滤
  4. 无 token 场景 → 401 → 跳登录（现有守卫已处理）

## 不做
- inspector 按钮 mutation（P1-2c）
- 创建任务对话框接 API（P1-2c）
- allowedActions 按钮驱动（P1-2c）
- 删除 mock 数据（AppContext.tasks 其他页面还在用）

## 验收
1. build + test 全绿
2. 浏览器实测 4 项通过（任务来自主工程真实 DB）
3. git commit + push

## 审查（流程铁律）
完成后调用 codex-review-and-fix skill 审查（手动模式——Codex 运行时不可用）；报告 高/中/低 + 修复建议，写 codex-review-p1-2b.md；审查发现修复后重新 build+test。
