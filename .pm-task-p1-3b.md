# 微任务 P1-3b：前端会话数据层 + NDJSON 流式读取

背景：P1-3a 已实现会话 REST（列表/创建/详情/删除/sendMessage）。主工程另有 /api/chat/stream（POST {conversationId, content} → NDJSON 流：user/status/delta/done/error 帧，鉴权走 session cookie，经 vite proxy 同源）。本任务：前端会话数据层 + 流式读取 + useStreamChat hook（供 P1-3c WorkspacePage 接入）。

## 范围（严格）

### 1. 类型（src/types.ts 追加）
```ts
export interface ConversationDto {
  id: string
  title: string
  repositoryId: string | null
  createdAt: string
  updatedAt: string
}
export interface MessageDto {
  id: string
  role: 'user' | 'assistant'
  content: string
  createdAt: string
}
export interface ConversationDetailDto extends ConversationDto {
  messages: MessageDto[]
}
```

### 2. api/conversations.ts（新文件，用 src/api/client.ts）
- `listConversations()` → `{ data: ConversationDto[], page }`（unwrap:false）
- `createConversation(input: { title?: string; repositoryId?: string | null })` → ConversationDto（idempotent）
- `fetchConversation(id)` → ConversationDetailDto
- `deleteConversation(id)` → 删除
- `sendMessage(id, content)` → `{ userMessage, assistantMessage }`（普通端点 fallback——非流式场景）

### 3. api/stream.ts（新文件——NDJSON 流式客户端）
- `streamChat(input: { conversationId: string; content: string }, handlers: { onUser, onStatus, onDelta, onDone, onError }, signal?: AbortSignal)`：
  - fetch POST `/api/chat/stream`（body {conversationId, content}，credentials same-origin）
  - res 非 2xx → 尝试读 NDJSON error 帧或 fallback → onError
  - ReadableStream + TextDecoder 按行（\n）分割 → 每行 JSON.parse → 按 type 分发（user→onUser / status→onStatus / delta→onDelta / done→onDone / error→onError）
  - 容错：单行解析失败跳过（不断流）；AbortSignal 中断
- 导出 `ChatStreamFrame` 类型

### 4. queries/conversations.ts（新文件）
- `conversationKeys`：`all: ['conversations']` / `detail(id)` / `lists()`
- `useConversations()`：useQuery(listConversations, select: data.data)
- `useConversation(id)`：useQuery(fetchConversation, enabled: !!id)
- `useCreateConversation()` / `useDeleteConversation()`：mutations + invalidate all
- `useSendMessage()`：mutateAsync(sendMessage) + invalidate detail(id)

### 5. useStreamChat hook（src/hooks/useStreamChat.ts 新文件）
- 输入：conversationId
- 状态：`{ assistantText, status: 'idle'|'streaming'|'done'|'error', error }`
- `start(content: string)`：清空 assistantText → streamChat 调起 → onDelta 累加 → onDone 置 done（调用方 invalidate 刷新持久消息）
- `abort()`：AbortController.abort
- 暴露 reset（切换会话时清空）

### 6. 测试（src/api/stream.test.ts + src/hooks/useStreamChat.test.tsx）
- stream.test：mock fetch 返回 ReadableStream（NDJSON 多行：user/status/delta×2/done）→ 断言 handlers 顺序 + 参数；单行坏 JSON 跳过不断流；非 2xx → onError
- useStreamChat.test：mock streamChat → start 后 assistantText 累加、status 流转、abort

### 7. 验证
- `npm run build` + `npm test`（现有 25 个不回归；App.test.tsx 若因新 import 失败调整）
- 浏览器实测（dev 4173）：
  1. console 调 `streamChat({conversationId: <真实id>, content: '你好'})` → onDelta 逐字回调 + onDone（NDJSON 解析正确）
  2. 错误路径：非法 conversationId → onError

## 不做
- WorkspacePage 会话面板接入（P1-3c）
- 打字机 UI（P1-3c 由 WorkspacePage 现有组件消费 useStreamChat）

## 验收
1. build + test 全绿
2. 浏览器实测 2 项通过（真实流式回调）
3. git commit + push

## 审查（流程铁律）
完成后调用 codex-review-and-fix skill 审查（手动模式）；报告 高/中/低 + 修复建议写 codex-review-p1-3b.md；修复后重新 build+test。
