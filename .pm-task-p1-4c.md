# 微任务 P1-4c：WorkspacePage Git 记录 + 变更审查接 REST（revert 按钮）

背景：P1-4a/b 已实现仓库 REST + 前端数据层（useRepositories/useCommits/useChanges/useRevertChange——revert mutation 已预埋 queries/repositories.ts）。本任务：WorkspacePage 的 ExplorerPane Git 记录 tab + 变更审查抽屉（ChangesDrawer 区域）从 AppContext mock 迁移到真实后端。

## 范围（严格）

### 1. REST 仓库 id 来源
- WorkspacePage 顶部新增 `const { data: repos } = useRepositories()`（queries/repositories.ts）
- **restRepoId** = repos?.[0]?.id（MVP：第一个仓库 repo-cc-main；项目切换器 UI 与 REST 仓库映射放 P2——在代码注释标注）
- Git 记录 tab 与变更审查都用 restRepoId（useCommits(restRepoId, enabled)/useChanges(restRepoId, enabled)——enabled = !!restRepoId && !restRepoIdDisabled(仓库无 localPath 时后端 400——用 hasLocalPath 字段判断)）

### 2. ExplorerPane Git 记录 tab（约 104-114 行）
- hardcoded 数组（c83b6e1/feat: 接入项目文件预览…）→ `useCommits(restRepoId)` 数据
- CommitDto 字段：hash/message/author/date——渲染 hash 前 7 位 + message + author + 相对时间（formatCommitTime 已有？——复用 RepositoriesPage 的时间格式化或简单 date 字符串）
- 加载态：isLoading → 骨架/提示；空 → "暂无提交记录"
- 文件树 tab（repositoryTree mock）**保持不变**（主工程暂无文件树端点——P2 标注注释）

### 3. 变更审查抽屉（ChangesDrawer 区域，约 469-529 行）
- `useApp().changes/reviewChange`（mock）→ `useChanges(restRepoId)` 数据 + `useRevertChange` mutation
- WorktreeChangeDto 字段：path/changeType/addedLines/deletedLines（P1-4a 契约）——映射到 UI（changeType: added/modified/deleted → 标签色；diff 统计）
- 待处理计数：changes.filter(changeType !== 'deleted'?) 或按 status——用真实数据计算
- **revert 按钮**：每项变更加"还原"按钮（canReview = useCapability('vcs:revert') 控制显隐）→ `revertChange.mutate({ id: restRepoId, path })` → onSuccess invalidate changes（repositoriesKeys.changes(id)——**注意**：useRevertChange 的 onSuccess 已 invalidate detail(vars.id)——确认 invalidate 覆盖 changes 前缀——若无则补）
- reviewChange（mock 记录审查反馈）→ 保留 UI 但标注"审查反馈持久化 P2"或接 useRevertChange 之外的逻辑？——**决定**：reviewChange mock 移除——变更审查操作统一走 revert（MVP 真实操作）；"批准"类动作标注 P2
- 移除：mock changes 依赖（AppContext changes/reviewChange）

### 4. 验证
- npm run build + npm test（App.test.tsx workspace 测试若挂——vi.mock queries/repositories 已在？——检查；mock 需含 useRepositories/useCommits/useChanges/useRevertChange）
- 浏览器实测：
  1. /workspace → Git 记录 tab → **真实 git log**（P1-4a 提交消息）
  2. 变更审查抽屉 → 真实工作区变更（.pm-task 文件 added 等）
  3. 还原按钮 → POST revert → changes 刷新（文件消失/状态变化）
- git commit + push

## 不做
- 文件树 REST（主工程无端点——P2）
- 项目切换器 ↔ REST 仓库映射（P2）
- 审查反馈持久化（P2）

## 审查（流程铁律）
完成后调用 codex-review-and-fix skill 审查（手动模式）；报告写 codex-review-p1-4c.md；修复后重新 build+test。

**开工前先用 mcp__codegraph__codegraph_explore 探索**（查询 "ExplorerPane Git 记录 tab WorkspacePage; ChangesDrawer changes reviewChange useApp; useCommits useChanges useRevertChange queries/repositories" 等，项目根 C:\Users\13588\Desktop\AI_Coding_Center）。
