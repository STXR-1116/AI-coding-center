# 微任务 P2-2b：KnowledgePage + SkillsPage 接 REST

背景：P2-2a 已实现知识库/Skill REST（KB 4 route + skills 5 route）。本任务：前端 KnowledgePage（src/pages/KnowledgePage.tsx）+ SkillsPage（src/pages/SkillsPage.tsx）从 mock 迁移。

## 范围（严格）

### 1. 数据层（参照 P2-1b agents 模式）
- **src/types.ts**：KnowledgeBaseDto（按 knowledge-dto.ts 实际字段）+ SkillDto（按 skill-dto.ts 实际字段）
- **src/api/knowledge.ts**：listKnowledgeBases/fetchKnowledgeBase/registerKnowledgeBase/updateKnowledgeBase/disableKnowledgeBase/bindAgent/unbindAgent（列表 unwrap:false）
- **src/api/skills.ts**：listSkills/fetchSkill/createSkill/updateSkillManifest/deprecateSkill/reactivateSkill/bindAgent/unbindAgent
- **src/queries/knowledge.ts** + **src/queries/skills.ts**：keys（lists/detail）+ useKnowledgeBases/useKnowledgeBase + useSkills/useSkill + mutations（register/update/disable/bind/unbind + create/update/deprecate/reactivate/bind/unbind——onSuccess invalidate lists+detail）——disabled key 工厂模式（P1-4b 同款）

### 2. KnowledgePage 迁移
- 列表：useState(initialKnowledgeBases) → useKnowledgeBases()（前端过滤 query/health）
- 详情：selectedId → 列表项渲染 + mutation 后 invalidate
- 注册表单：→ useRegisterKnowledgeBase（POST 幂等）→ 关闭 + invalidate + toast
- 停用：→ useDisableKnowledgeBase（mutate id → toast"已停用"）
- 绑定 Agent（页面若有绑定下拉）：→ useBindKnowledgeBase（POST agents/{agentId}）——**看页面结构决定**：若绑定 UI 复杂则保留本地提示（P2-1b toggleSquadMember 模式——"待后端接入"改为真实 mutation）
- 移除 initialKnowledgeBases 依赖；加载/错误/空态

### 3. SkillsPage 迁移
- 列表：useState(initialSkills) → useSkills()（前端过滤）
- 详情/manifest：→ useSkill(id)（或列表项渲染）
- 创建表单：→ useCreateSkill（POST 幂等）
- 版本操作：deprecate/reactivate 按钮 → 对应 mutation（toast + invalidate）
- 绑定（bindings）：→ 真实 bind/unbind mutation 或本地提示（同 KB 决定）
- 移除 initialSkills/initialBindings 依赖；加载/错误/空态

### 4. 验证
- npm run build + npm test（App.test 若挂 → vi.mock queries/knowledge + queries/skills——参照 agents mock 模式）
- 浏览器实测（dev 4173 + 3003）：
  1. /knowledge → 真实 KB 列表（当前 0 → 空态）+ 注册流程（建一个测试 KB → 列表出现 → 停用 → 清理）
  2. /skills → 真实 skill 列表（python-basics）+ 详情
  3. deprecate/reactivate 操作（python-basics 真实操作——**注意**：改真实 skill 状态——验证后切回）
- git commit + push

## 不做
- credentials 编辑 UI（P2-3）
- getSkillsForTask 关联
- 知识库 health-check 触发

## 审查（流程铁律）
完成后调用 codex-review-and-fix skill 审查（手动模式）；报告写 codex-review-p2-2b.md；修复后重新 build+test。

**开工前先用 mcp__codegraph__codegraph_explore 探索**（查询 "KnowledgePage initialKnowledgeBases setKnowledgeBases mock KnowledgeBase; SkillsPage initialSkills SkillRecord bindings; knowledge-dto skill-dto 字段" 等，项目根 C:\Users\13588\Desktop\AI_Coding_Center）。
