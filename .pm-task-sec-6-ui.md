# 微任务 SEC-6-UI：前端安全 UI 接线（审计导出 + token 轮换）

背景：SEC-6 主工程已落地 rotate-token + audit export API——前端无 UI。本任务：接线两个管理功能。

## 范围（严格）

### 1. 审计导出按钮（src/pages/AnalyticsPage.tsx——审计日志 tab）
- 审计 tab 头部加"导出 CSV"按钮（Download 图标——参照现有按钮样式）
- src/api/audit.ts（或现有 audit api 文件）加 exportAuditLogs(): Promise<Blob>——fetch GET /audit-logs/export（带 cookie——凭证）→ res.blob()
- 点击 → 创建 objectURL → a[download]="audit-logs-<时间戳>.csv" 触发下载 → revokeObjectURL
- 下载失败（401/403/500）→ handleApiError toast（不静默）
- 主工程 export 是 GET（browser 直接下载也行——但走 fetch 可统一错误处理——**决定**：fetch + blob（错误处理统一））

### 2. Token 轮换按钮（src/pages/AgentsPage.tsx——详情面板）
- 详情面板底部操作区（"检查心跳"/"停用实例"旁）加"轮换 Token"按钮（RefreshCw 图标——管理角色可见：useSession role !== 'EMPLOYEE'）
- src/api/agents.ts 加 rotateAgentToken(id) → POST /agents/{id}/rotate-token
- src/queries/agents.ts 加 useRotateAgentToken（onSuccess: 复制 token 到剪贴板 + notify "新 Token 已复制（仅显示一次）" + invalidate agents detail/list）
- **注意**：新 token 明文只返回一次——onSuccess 立即 navigator.clipboard.writeText + toast 提示（不存 state/不渲染明文）

### 3. 测试
- 各加 2-3 用例（导出按钮渲染+点击触发下载 mock；轮换按钮角色可见性 + 点击复制）

### 4. 验证
- npm test 全量 + build
- 浏览器实测：/analytics 审计 tab → 点导出 → 文件下载（或 mock）；/agents 详情 → 轮换 Token → 剪贴板 + toast + agent 正常
- commit + push

## 不做
- 主工程改动（API 已就绪）
- 轮换确认弹窗（点按即转——管理操作）
