# 微任务 P1-2c：TaskInspector 写操作迁移（mutation + allowedActions）

背景：P1-2a/b 已完成数据层 + 读路径。本任务：TaskInspector（TasksPage.tsx 264-365 行）的写操作从 AppContext mock（updateTaskStatus/updateTaskMode）迁移到 REST mutation，按钮由 **allowedActions**（后端返回的资源级权限）驱动。

## 范围（严格）

### 1. primaryAction → useExecuteTask mutation
- `useExecuteTask()`（src/queries/tasks.ts 已有）替换 updateTaskStatus
- 主按钮点击 → `executeMutation.mutate(task.id, { onSuccess, onError })`
- onSuccess：按 task.status 变化 notify（awaiting_approval → '审批已通过，任务进入执行队列。' title '任务已批准'；pending/assigned/failed → '任务已启动。' title '任务已启动'）——**读返回的 task.status 决定文案**（避免猜测）
- onError：`handleApiError(e)`（queries/tasks.ts 已有）→ notify 错误 toast（tone: 'error'）

### 2. 按钮由 allowedActions 驱动（替换 status 推断）
- `const actions = task.allowedActions ?? []`（toTask 映射时兜底空数组）
- 主按钮：
  - `actions.includes('approve')` → 「批准执行」（icon Check）
  - `actions.includes('execute')` → 「开始执行」（icon Play）
  - 否则 → 不显示主按钮（running/terminal 态由后端动作集决定）
- 取消按钮：`actions.includes('cancel')` → 「取消」
- 移除旧的 `['pending','assigned',...].includes(task.status) && canExecute` 组合推断（allowedActions 已是后端按角色+状态算好的——**useCapability 兜底**：allowedActions 为空时回退 useCapability（task:execute/task:cancel）防止旧数据无 actions 导致按钮消失——toTask 映射时 allowedActions ?? useCapability 无法在映射层用 hook——**改为**：按钮条件 `(actions.includes('approve') || actions.includes('execute')) || canExecute`——简单兜底）

### 3. cancelTask → useCancelTask mutation
- `cancelTask(task.id, '用户取消')` → onSuccess notify '任务已停止，未完成的执行资源将被释放。'（title '任务已取消', tone warning）；onError 错误 toast

### 4. changeMode（执行模式）——诚实处理
- 后端 PATCH /tasks/{id} 暂不支持 executionMode（仅 tokenBudget）——点击模式按钮 → notify('执行模式暂不支持在线修改，将在后续版本支持。', { tone: 'info' })，**不再调用 mock updateTaskMode**（删除假成功）

### 5. 清理
- TaskInspector 不再用 `useApp().updateTaskStatus/updateTaskMode`（保留 useApp() 其他用途）
- canExecute/canCancel（useCapability）保留作兜底

### 6. 验证
- `npm run build` + `npm test`（App.test.tsx 的 useExecuteTask/useCancelTask mock 已有——mutateAsync vi.fn 不触发 onSuccess？——检查测试"adds execution events"点"标记完成"——现在按钮按 allowedActions：mock 任务 allowedActions 含 'execute' → 显示"开始执行"（不是"标记完成"）——**测试需调整**：mock 任务 allowedActions 加 'execute'，测试点"开始执行"→ mutateAsync 被调（断言 mock 被调 + 无 toast？）——或者测试改为验证按钮渲染。调整测试使其匹配新行为）
- 浏览器实测：
  1. 选 pending 任务 → 主按钮"开始执行"（allowedActions 含 execute）
  2. 点开始执行 → POST /tasks/{id}/execute → 后端状态流转（pending→assigned→running？——executeTask service 实际行为）→ 按钮消失（running 无 execute action）→ toast 提示
  3. 选已完成任务 → 无主按钮 + 无取消（allowedActions 空）
  4. 取消按钮 → POST cancel → 状态 cancelled

## 不做
- CreateTaskDialog 接 API（P1-2d，需要 requirementId 数据源）
- executionMode 在线修改（后端 PATCH 未支持）
- TaskEventTimeline 接 events API（P1-3）

## 验收
1. build + test 全绿
2. 浏览器实测 4 项通过（真实后端状态流转）
3. git commit + push

## 审查（流程铁律）
完成后调用 codex-review-and-fix skill 审查（手动模式）；报告 高/中/低 + 修复建议写 codex-review-p1-2c.md；修复后重新 build+test。
