# 微任务 P2-4b-fix：审查修复（H1/M1/M2/M3）

背景：codex-review-p2-4b.md 审查发现 1 高 4 中 5 低。修 H1（必须）+ M1-M3（AI_Coding_Center src/）。

## H1（必须）：useMetricsSummary query key 每渲染漂移 → 无限 refetch
- src/queries/dashboard.ts:76-88：hook 体内每次渲染 `new Date()` 算 from → key 漂移
- 修：`useMemo(() => ({ ...params, range }), [range, JSON.stringify(params)])` 稳定 mergedParams（或 from 降粒度到天——**决定**：useMemo 稳定）

## M1：App.test 补 mock
- src/App.test.tsx：vi.mock('./queries/dashboard') + vi.mock('./queries/users')（**模块级稳定引用**——P2-3b 教训：内联对象触发无限重渲染）

## M2：审计 result 启发式推断
- AnalyticsPage/UsersPage 审计 result 渲染：/fail/i 启发式 → 用后端字段（看 audit-dto 有无 result 字段——有则直接用；无则保留但标注）

## M3：AuditActor 死分支
- src/types.ts：AuditActor 移除 'service' 类型（数据源永不可能产出）

## 验收
1. npm run build + npm test 全绿
2. 浏览器实测：/analytics 无无限 refetch（network 无持续请求）
3. git commit + push
