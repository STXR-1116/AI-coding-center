# 微任务 P2-3b-fix：契约对齐 + 接线收尾

背景：P2-3b CC 半途——SettingsPage 的 saveBudget 逻辑已写但（a）TokenBudgetConfig 类型仍是旧 4 字段（monthlyTokenBudget/singleTaskTokenLimit/budgetWarningThreshold/base——P2-3a 后端实际是 base/per100Chars/min/max）（b）保存/重置按钮未接线（budgetDirty/saveBudget/resetBudget unused）（c）RepositoriesPage 注册/测试未接。AI_Coding_Center src/。

## 1. 契约对齐（types.ts）
- TokenBudgetConfig 改为后端 config-dto 形状：base/per100Chars/min/max（全 number，正数）
- SettingsPage 202 行（budgetForm.monthlyTokenBudget 进度条）——改用 base 或移除该进度条（看 UI 上下文——进度条显示"月度预算用量"——后端无 monthly 概念 → 移除该进度条或改为显示 base 值）

## 2. SettingsPage 运行与预算 tab 接线
- 保存按钮 → saveBudget（disabled 用 !budgetDirty || isPending）
- 重置按钮 → resetBudget（disabled !budgetDirty）
- 表单四个字段（base/per100Chars/min/max）→ updateBudget
- 加载态：useTokenBudgetConfig isLoading → 骨架/禁用表单

## 3. RepositoriesPage 注册 + 测试
- 接入表单（当前占位 toast"仓库注册能力即将上线"）→ registerRepository（POST /repositories——api/repositories.ts 可能已有——没有则加）→ onSuccess invalidate + toast + 关闭
- 详情面板加"测试连接"按钮 → testRepository(id)（api 已补）→ 结果 toast

## 4. 验收
1. npm run build + npm test（App.test 若挂 → vi.mock queries/config + repositories mock 扩展）
2. 浏览器实测：/settings 预算 tab 真实值 + 保存闭环；/repositories 注册/测试
3. git commit + push
