# 微任务 P1-3c-fix：会话面板审查修复（H1 + M1-M4）

背景：P1-3c 审查（codex-review-p1-3c.md）发现 1 高 4 中。修复清单如下，按优先级。

## H1（高）：OutputPane 仍读 useApp() mock conversations → 真实后端下产出面板永久空态
- src/pages/WorkspacePage.tsx 的 OutputPane（约 258 行起）：`const { selectedConversationId, conversations, ... } = useApp()`——conversations 来自 AppContext mock
- 修：OutputPane 的会话查找改为 REST 数据——`useConversations()`（queries/conversations.ts）拿列表 + selectedConversationId（保留 useApp）——`const conversation = conversations?.find(c => c.id === selectedConversationId)`（REST 列表）
- OutputPane 其余逻辑（requirements/tasks 聚合——getConversationOutputsAction 已接 REST）不动
- 验证：浏览器实测选会话 → 产出面板显示该会话产出（非空态）

## M2（中）：retry 依赖 draft 但 sendDraft 已清空 → 重试按钮恒禁用
- StreamingChat 的 retry（约 290 行）：`if (!draft.trim()) return`——draft 已清 → 恒 return
- 修：错误时保存 lastFailedContent（ref 或 state），retry 用 lastFailedContent 重发（或 retry 直接调 chat 重发上一条——用 ref 存最后发送内容）

## M3（中）：无会话连发竞态（无"创建中"守卫，pendingSend 被覆盖）
- handleNeedConversation（父组件）：连点发送 → 两次 setPendingSend + 两次 createConversation
- 修：isCreatingRef 守卫（创建中忽略新投递）或 pendingSend 非空时忽略

## M4（中）：done 后 assistantText 未清空 → 与 refetch 持久消息短暂重复渲染
- StreamingChat：chat.status === 'done' 后 onStreamDone 触发 invalidate → useConversation refetch 返回持久消息（含 assistant 回复）——此时 chat.assistantText 仍显示 → 重复
- 修：done 后清 assistantText——useStreamChat 增加 done 后清空（onDone 里 setAssistantText('')？——但 abort 也要保留文本……——方案：useStreamChat 的 onDone 在设置全文后不立即清；**由 StreamingChat 在 status 变 done 的 effect 里清**（onStreamDone 前）——或 useStreamChat 提供 clearText()；最简单：StreamingChat 的终态 effect 里 `chat.reset()`？——reset 清 idle 也清文本——done 后 reset 使状态回 idle（下次 start 正常）——但 abort 场景保留文本？——**统一**：done/error 后 StreamingChat 调 reset()（清流式态；持久消息由 refetch 提供）——abort 场景（用户手动停）保留文本（不 reset）

## M1（中）：删除后 selectConversation('') → 改为 null
- removeConversation：选中会话被删 → selectConversation('') → 改 null（AppContext 类型 string | null）

## 不做
- L 级（prevStatusRef 初值/时区/滚动/测试缺口）记录

## 验收
1. npm run build + npm test（39 全绿不回归；App.test 若受 H1 影响调整——OutputPane 用 useConversations mock（已有））
2. 浏览器实测：选会话 → 产出面板非空（真实产出）；重试按钮在错误后可点；done 后无重复消息
3. git commit + push
