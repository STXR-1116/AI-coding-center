# 微任务 P0-3a：登录页 + 路由（极小任务）

仅两个文件改动（src/pages/LoginPage.tsx 新建 + src/App.tsx 改路由）。API 层已完成（src/api/client.ts + src/api/auth.ts 存在）。

## 做什么

### 1. src/pages/LoginPage.tsx（新建）
居中卡片登录页：
- 品牌标记（public/coding-center-mark.svg）+ 标题"CodingCenter"
- 用户名输入 + 密码输入 + 登录按钮（提交中 disabled + "登录中…"）
- 错误提示（红色文本）
- 调用 `login(username, password)`（src/api/auth.ts 已有，POST /auth/login，返回 { user, capabilities }）；成功 → `window.location.href = "/tasks"`（简单跳转，不依赖 Context）
- 失败 → 显示 `err.message`
- 样式：用 styles.css 现有 token（background/卡片圆角/主按钮色），参考现有页面布局风格，内联 style 或独立 css

### 2. src/App.tsx（改）
- import LoginPage + lazy 加载
- 加路由 `{ path: "/login", element: <LoginPage /> }`（放在 / 与 /tasks 之间，无 ModuleGate）
- **其余不动**（RequireAuth/Context 是 P0-3b）

## 验收
1. `npm run build` 通过
2. 不改其他文件（除了这两个）
