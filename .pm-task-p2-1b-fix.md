# 微任务 P2-1b-fix：审查修复（M1 + L1 + L5）

背景：codex-review-p2-1b.md 审查发现 0 高 1 中 5 低。修 M1/L1/L5（AI_Coding_Center src/）。

## L1（用户可见语义）：运行位置按钮空 PATCH 谎报成功
- src/pages/AgentsPage.tsx 约 389 行："本地/云端"按钮传 { status: 当前status }（空 PATCH）却 toast"运行位置已记录"——后端 DTO 无 runtime 字段，刷新必回退
- 修：与"检查心跳"一致——降级为 info toast（"运行位置切换待后端接入"）+ 按钮禁用或仅提示，**不发空请求**

## M1（类型收敛）：executionMode 散落三处
- toAgent 用交叉类型挂 executionMode/periodResetAt + inspector 两处 as 读回
- 修：把 executionMode 正式并入 UI 域 Agent 接口（src/types.ts 或页面局部类型——看 Agent 类型定义位置）——消除重复断言；periodResetAt 无消费点 → 保留字段但标注或移除（**决定**：保留——周期 Token 展示需要，只是当前页面展示"0 已用共 0"用的其他字段——检查后若真无消费则移除）

## L5（测试债）：补 toAgent 纯函数单测 + App.test 预防性 mock
- 新建 src/api/agents.test.ts（toAgent 枚举兜底/字段映射——纯函数测试，mock client 的 get/post）
- src/App.test.tsx 顶部预置 vi.mock('./queries/agents', ...)（返回空 agents——防御未来 /agents 用例裸 fetch——与 useRealtimeEvents 预防性 mock 同思路）

## 验收
1. npm run build + npm test（57+ 全绿不回归）
2. git commit + push
