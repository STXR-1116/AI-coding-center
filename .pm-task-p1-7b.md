# 微任务 P1-7b：前端 SSE 实时事件客户端

背景：P1-7a 已实现 GET /api/v1/events（SSE：hello/keep-alive/task.status_changed/task.execution_progress 事件帧，EventSource 兼容，cookie 鉴权）。本任务：前端订阅 + 事件驱动缓存失效——任务状态变更自动刷新，不再依赖手动刷新。

## 范围（严格）

### 1. 实时事件 hook（src/hooks/useRealtimeEvents.ts）
- `useRealtimeEvents(enabled: boolean)`：enabled 时创建 `new EventSource('/api/v1/events')`（同源自动带 session cookie）
- 事件处理（JSON.parse event.data → 按 type 分发）：
  - `task.status_changed` → invalidate queries/tasks 的列表 + 详情 key（taskKeys——参照 src/queries/tasks.ts 的 key 工厂——列表 + `taskKeys.detail(taskId)`；同时全局 invalidate lists 前缀）
  - `task.execution_progress` → invalidate task 详情
  - `approval.created` / `approval.resolved` / `agent.health` → 预留：console.debug 记录（MVP 无对应 UI）
- 生命周期：enabled 变 false（登出/未登录）→ close；组件卸载 → close；onerror → 浏览器 EventSource 自动重连（默认行为——不手动 close）；**重连防抖**：onerror 连续触发时 backoff 由浏览器处理（EventSource 原生重连）——只需确保不重复创建实例（useRef 持有单例）
- 返回 { connected: boolean }（onopen 置 true / onerror+close 置 false——可选）

### 2. 接入点（src/App.tsx 或 AppShell）
- 登录后（auth.user 存在）→ `<RealtimeBridge />` 或 App 内 `useRealtimeEvents(!!auth.user)`——**位置**：App 顶层（路由外，登录后常驻订阅）
- 需要 useQueryClient——放 App.tsx 内小组件（App 已在 QueryClientProvider 内）

### 3. 验证
- npm run build + npm test（新测试：src/hooks/useRealtimeEvents.test.tsx——mock EventSource（fake class 触发 onopen/onmessage/onerror）+ 断言 invalidate 被调（mock useQueryClient 或注入 queryClient））
- 浏览器实测（dev 4173 + 3003）：
  1. 登录 → /tasks → 打开页面
  2. 另一终端 curl 取消一个任务（POST /tasks/{id}/cancel——用测试任务）
  3. 任务列表**自动刷新**（该任务状态变已取消，无需手动刷新）——network 里看到自动重拉 /api/v1/tasks
  4. 登出 → SSE 断开（network 无持续连接）
- git commit + push

## 不做
- approval/agent.health 事件 UI（预留）
- WS 通道
- 通知铃声/红点（后续）

## 审查（流程铁律）
完成后调用 codex-review-and-fix skill 审查（手动模式）；报告写 codex-review-p1-7b.md；修复后重新 build+test。

**开工前先用 mcp__codegraph__codegraph_explore 探索**（查询 "taskKeys queries/tasks key 工厂 invalidate; App.tsx 结构 auth user QueryClientProvider; useRealtimeEvents" 等，项目根 C:\Users\13588\Desktop\AI_Coding_Center）。
