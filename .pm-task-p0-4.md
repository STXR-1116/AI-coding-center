# 微任务 P0-4：前端权限模型改造（capabilities 驱动，删 role 硬判）

背景：前端工程（C:\Users\13588\Desktop\AI_Coding_Center）已接入 auth（AppContext.auth.capabilities: string[]，主工程返回 task:assign/task:execute/task:cancel/vcs:revert/module:toggle/requirement:analyze 等）。README 契约要求：**按钮显隐/禁用由 capabilities 驱动，不得仅按 role 推导**。本任务改造 3 个页面的硬判。

## 范围（严格）

### 1. 新建 hook（src/state/useCapability.ts）
- `useCapability(cap: string): boolean`——`useApp().auth.capabilities.includes(cap)`
- 导出 `can(user?, ...)` 不需要——只做 hook

### 2. SettingsPage.tsx（改 2 处）
- line 155 `const canManageModules = user.role === 'leader' || user.role === 'pm'` → `const canManageModules = useCapability('module:toggle')`
- 访问 banner（"可管理/只读"文案）同样用 canManageModules
- 模块开关 SettingSwitch：`!canManageModules` 时 disabled（无 module:toggle 只能看不能切）

### 3. TasksPage.tsx（改操作按钮）
- 底部操作按钮区（批准执行/开始执行/标记完成/取消）：
  - "批准执行/开始执行"（awaiting_approval/running 相关）→ 需 `task:execute`
  - "取消"按钮 → 需 `task:cancel`
  - 无对应 capability → 按钮隐藏（不是 disabled）
  - 保持原有 status 条件（status 条件 + capability 双条件：`status===x && useCapability('task:execute')`）

### 4. WorkspacePage.tsx（改 2 处）
- OutputPane "批准执行"按钮（line 302 附近，`task.status === 'awaiting_approval'`）→ 加 `useCapability('task:execute')`
- DiffDrawer "接受变更/拒绝并回滚" → 需 `vcs:revert`（无则按钮隐藏）
- 顶部"切换云端"（cloudFallback 本地 state）→ 需 `agent:manage`（隐藏无能力者）

## 不做
- 不改后端（capabilities 已由 me 返回）
- 不做资源级 allowedActions（P1 接 API 时资源返回再细化）
- 不改 mock 数据/其他页面

## 验收
1. `npm run build` 通过 + `npm test` 全绿（App.test.tsx 的 mock capabilities 已含 task:execute/vcs:revert/module:toggle——如果测试断言受影响需同步调整 mock）
2. 浏览器实测（dev 4173，leader 登录）：
   - /settings 模块开关可操作（leader 有 module:toggle）
   - /tasks 操作按钮按 leader capabilities 显示
   - /workspace 变更审查按钮可用（leader 有 vcs:revert）
   - （employee 登录验证隐藏——如种子有 employee 账号可试）
3. git commit（前端工程）
