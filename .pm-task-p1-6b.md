# 微任务 P1-6b：前端需求数据层补全 + RequirementsPage 接 REST

背景：P1-2d 已建 api/requirements.ts（listRequirements/fetchRequirement + useRequirements）+ P1-6a 主工程已实现需求写 REST（POST /requirements + /{id}/analyze + GET /{id}/specs + POST /{id}/cancel）。本任务：补全数据层写操作 + RequirementsPage（src/pages/RequirementsPage.tsx）从 AppContext mock 迁移。

## 范围（严格）

### 1. 数据层（参照 P1-2a/P1-4b 模式）
- **src/types.ts**：补 RequirementSpecDto（version/title/body/createdAt/status——按主工程 requirement-spec-dto.ts 实际字段）+ AnalysisResult（按 P1-6a analyze 返回）+ RequirementListParams 已有（确认含 status/query）
- **src/api/requirements.ts**：补 createRequirement(input)（POST + Idempotency-Key 自动——client 已带）/ analyzeRequirement(id)（POST）/ listRequirementSpecs(id)（GET）/ cancelRequirement(id)（POST）——复用 client + unwrap
- **src/queries/requirements.ts**：补 keys（specs）+ useRequirement(id)（enabled 门控——参照 P1-4b useRepository 的 disabled key 模式）+ useRequirementSpecs(id) + useCreateRequirement（onSuccess invalidate lists）/ useAnalyzeRequirement（onSuccess invalidate detail(id)+specs(id)）/ useCancelRequirement（onSuccess invalidate lists+detail）

### 2. RequirementsPage 迁移
- **列表**：`useApp().requirements` → `useRequirements()`（已有 hook——select 解包 {data}；返回 RequirementDto[]）
- **详情**：selectedId → 详情数据用 useRequirement(selectedId)（fetchRequirement 已有——或直接用列表项（列表已含字段）——**决定**：详情面板用列表项渲染（不额外请求）+ 规格区用 useRequirementSpecs(selectedId)）
- **创建表单**（handleCreate + addRequirement mock）：→ useCreateRequirement().mutateAsync({ title, description, priority })（**注意**：主工程 createRequirement 必填 title/summary——description 映射 summary）→ onSuccess 关闭对话框 + invalidate 列表 + 选中新需求 id
- **分析按钮**（"AI 分析"——页面若有）：→ useAnalyzeRequirement().mutate(id) → onSuccess invalidate specs + toast"分析完成，Spec 已生成"
- **版本化区**（specExpanded 展开区——当前 mock spec）：→ useRequirementSpecs(selectedId) 渲染版本列表（version + title + body + createdAt）；加载/空态
- **取消按钮**（详情面板若有）：→ useCancelRequirement().mutate(id) → onSuccess invalidate + toast
- **statusOverrides mock 移除**（真实状态流转——status 用 RequirementDto.status）
- 保留：搜索/状态/项目过滤（前端过滤 useRequirements 结果——同 P1-2b 模式）；模板（templates）保留但描述进 summary
- 移除：addRequirement/statusOverrides 依赖

### 3. 验证
- npm run build + npm test（App.test.tsx 若挂——vi.mock queries/requirements 扩展：useRequirement/useRequirementSpecs/useCreateRequirement 等返回 mock——参照 conversations/repositories mock 模式）
- 浏览器实测（dev 4173 + 3003）：
  1. /requirements → 真实需求列表（主工程 DB）
  2. 搜索/状态过滤前端生效
  3. 详情面板：真实字段 + 规格区（版本列表——有 spec 的需求显示 v1；无 spec 显示空态）
  4. 新建需求 → POST → 列表刷新 + 新需求选中
  5. 分析按钮 → POST analyze → specs 出现 v1
  6. 取消按钮 → POST cancel → 状态变已取消
- git commit + push

## 不做
- 审批流 UI（P2）
- WorkspacePage 需求相关（产出面板聚合 P2）

## 审查（流程铁律）
完成后调用 codex-review-and-fix skill 审查（手动模式）；报告写 codex-review-p1-6b.md；修复后重新 build+test。

**开工前先用 mcp__codegraph__codegraph_explore 探索**（查询 "RequirementsPage requirements addRequirement statusOverrides handleCreate; useRequirements queries/requirements; createRequirement analyzeRequirement listRequirementSpecs cancelRequirement api/requirements" 等，项目根 C:\Users\13588\Desktop\AI_Coding_Center）。
