# 微任务 P2-2b-fix：审查修复（高-1 + 中-1/2/3）

背景：codex-review-p2-2b.md 审查发现 1 高 3 中 2 低。修高-1（必须）+ 中-1/2/3。

## 高-1（功能性）：KnowledgePage Agent 绑定状态永远"未绑定"
- 根因：列表桥 toKnowledgeBase 恒返回 boundAgents: []（列表 DTO 只有 boundAgentCount），详情桥 toKnowledgeBaseDetail + useKnowledgeBase **零调用**——页面只读列表从不取详情
- 修：KnowledgePage 详情面板选中 KB 时调 useKnowledgeBase(selectedId)（enabled 门控）→ 详情数据（含 boundAgents）驱动绑定开关渲染（checked=boundAgents.includes(agentId)）
- 对照：SkillsPage 用 toBoundAgentIds 正确渲染（skill 列表 DTO 自带 boundAgents）——保持两页一致

## 中-1：详情 hooks 死代码（与高-1 同根）——接入 useKnowledgeBase 即解决

## 中-2：testConnection 静默吞参 + 误导性按钮
- 看页面现状：testConnection 用 void knowledgeBase 吞参——修：按钮文案/disabled 反映"待后端接入"（诚实降级——参照 P2-1b 运行位置按钮模式：info toast + 不误导）

## 中-3：停用后无重新启用路径
- KnowledgePage：停用成功后按钮区显示"已停用"态 + 提供"重新启用"入口（若后端 disable 后 update 可恢复——**决定**：disable 后详情面板显示"已停用"标签 + 按钮禁用（重新启用待 P2-3 config 端点）——诚实标注）

## 不做
- 低-1/低-2（BindResult 类型标注 + App.test 预防性 mock——低-2 顺手加：vi.mock queries/knowledge + queries/skills）

## 验收
1. npm run build + npm test（57+ 全绿）
2. 浏览器实测：/knowledge 注册一个可达 KB（或用单测 mock 数据）→ 详情面板绑定开关按真实 boundAgents 渲染
3. git commit + push
