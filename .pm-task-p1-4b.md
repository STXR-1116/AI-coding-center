# 微任务 P1-4b：前端仓库数据层 + RepositoriesPage 接 REST

背景：P1-4a 已实现仓库 REST（列表/详情/commits/changes/revert）。本任务：前端数据层 + RepositoriesPage（src/pages/RepositoriesPage.tsx，当前用 useApp().projects mock + 前端过滤）迁移到真实后端。

## 范围（严格）

### 1. 数据层（参照 P1-2a 的 tasks 数据层模式）
- **src/types.ts**：RepositoryDto（id/name/description/status/enabled/hasLocalPath/visibility…）、CommitDto（hash/message/author/time…）、WorktreeChangeDto（path/changeType/addedLines/deletedLines…）——按 P1-4a 的 repository-dto.ts 实际字段
- **src/api/repositories.ts**：listRepositories（unwrap:false 拿 page）/ fetchRepository / listCommits / listChanges / revertChange——复用 api/client.ts
- **src/queries/repositories.ts**：useRepositories / useRepository(id) / useCommits(id) / useChanges(id) + useRevertChange mutation + keys 工厂 + invalidate 策略

### 2. RepositoriesPage 接 REST
- 数据源：useApp().projects（mock）→ useRepositories() + useCommits(id)/useChanges(id)（选中仓库详情）
- 保留：搜索/状态过滤 UI（改为前端过滤 REST 列表——同 P1-2b 模式：列表 REST 拉全量 + 前端 filter）
- 仓库卡片：真实 name/description/status/updatedAt
- 详情（选中仓库）：commits 列表（真实 git log）+ 变更统计（真实 changes）
- 移除：hardcoded commit 数组（line 50 mock）与 projects 依赖
- 加载/错误态：isLoading/error 展示（EmptyState）

### 3. 验证
- npm run build + npm test（App.test.tsx 若挂 → vi.mock queries/repositories 返回 mock 数据——参照 conversations mock 模式）
- 浏览器实测（dev 4173 + 3003）：
  1. /repositories → 真实仓库列表（Coding Center ×2）
  2. 搜索/状态过滤前端生效
  3. 选仓库 → 真实 commits（git log）+ changes 统计
  4. 未登录 → 跳登录（RequireAuth 已包）
- git commit + push

## 不做
- WorkspacePage 的 RepoExplorer/CommitList 迁移（P1-4c 单独）
- 仓库注册/管理表单（P2）
- revert 按钮接 API（P1-4c——本任务只读）

## 审查（流程铁律）
完成后调用 codex-review-and-fix skill 审查（手动模式）；报告写 codex-review-p1-4b.md；修复后重新 build+test。

**开工前先用 mcp__codegraph__codegraph_explore 探索**（查询 "RepositoriesPage 现状 repositories state projects useApp mock; tasks data layer api/queries pattern useTasks" 等，项目根 C:\Users\13588\Desktop\AI_Coding_Center）。
