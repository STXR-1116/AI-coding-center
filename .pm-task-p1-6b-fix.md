# 微任务 P1-6b-fix：审查修复（M1/M2/M3）

背景：codex-review-p1-6b.md 审查发现 3 中 3 低。修 3 个中项（AI_Coding_Center src/）。

## M1：项目筛选维度整体失效（projectId 永远不匹配）
- 根因：RequirementDto 无 projectId（主工程需求全局挂会话/提交人，createRequirement 也无 projectId 参数）——toRequirement 硬编码 projectId '' 导致项目筛选恒空 + 创建表单"所属项目"是死控件
- 修（方案 1）：摘掉项目维度——RequirementsPage 移除 projectId 筛选下拉 + 表单的"所属项目"字段（保留模板/优先级）；toRequirement 不再有 projectId 相关逻辑
- 注意：移除后相关 state（projectId）/过滤条件同步删

## M2：analyzing 死状态
- 根因：后端 /analyze 直接把 draft → in_progress（不经 analyzing 中间态）——前端 analyzing 状态不再产生
- 修：状态机里 analyzing 保留（兼容历史数据）但在 asStatus/下拉中归一兜底：analyzing 显示为"解析中"标签 + 详情面板对 analyzing 态需求**仍显示"解析需求"按钮**（允许重试推进——与 draft 同逻辑）——即按钮门控改为 status === 'draft' || status === 'analyzing'

## M3：App.test.tsx requirementsKeys mock 不完整
- requirementsKeys mock 只 { all: ['requirements'] }——补 lists()/details()/detail(id)/specs(id) 工厂（对齐真实 queries/requirements.ts）

## 不做
- L 级（specSummary 兜底/cancel 返回校验/latestSpecVersion 排序）记录后续

## 验收
1. npm run build + npm test（48 全绿不回归）
2. 浏览器实测：/requirements 项目筛选消失；草稿需求显示"解析需求"按钮可点
3. git commit + push
