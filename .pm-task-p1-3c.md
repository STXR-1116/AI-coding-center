# 微任务 P1-3c：WorkspacePage 会话面板接真实后端（列表/流式打字机/发送/新建/删除）

背景：P1-3a/b 已完成会话 REST + NDJSON 流式数据层（api/conversations.ts + api/stream.ts + useStreamChat + queries/conversations.ts）。本任务：WorkspacePage 的 ConversationPane（src/pages/WorkspacePage.tsx 内，约 115-200 行）从 AppContext mock（conversations/startConversation/sendMessage/completeMessage/deleteConversation + timerRef 假打字机）迁移到真实后端。

## 范围（严格）

### 1. 数据源
- `const { data: conversations } = useConversations()`（queries/conversations.ts）替换 `useApp().conversations`
- `useApp()` 保留：activeProjectId/selectedConversationId/selectConversation/projects（selectedConversationId 仍走 AppContext——OutputPane 也用）
- 会话过滤：`conversationsForProject` 的 projectId 过滤改为**不过滤**（REST 会话无 projectId——repositoryId 绑定后续 P 阶段；MVP 显示全部会话）

### 2. 发送 + 流式打字机（核心）
- 移除：`sendMessage`/`completeMessage`（AppContext mock）+ timerRef 假响应 + respondingConversationId 定时器
- 引入：`const chat = useStreamChat(conversationId)`（当前选中会话 id——**注意**：useStreamChat(conversationId) 的 conversationId 参数变化时 hook 内 reset——切换会话时 useStreamChat 需重新实例（key 或用 conversationId 依赖）——**方案**：ConversationPane 内 `<StreamingChat conversationId={id} />` 子组件（key={id} 强制重挂）或 useStreamChat 内部 useEffect 处理 conversationId 变化（hook 已依赖 [conversationId]——start 用最新值，但 assistantText 需在切换时清空——**子组件 key 方案最稳**）
- 发送流程 sendDraft：
  1. `draft.trim()` 非空 + `!chat.streaming`
  2. 无选中会话 → `createConversation()` 创建 → 用新 id
  3. `chat.start(message)` → 流式打字机
  4. onDone 后：invalidate `conversationKeys.detail(id)`（拉取持久消息）+ 会话列表 invalidate（updatedAt 变化）→ notify 可选
  5. 清空 draft
- 消息展示：持久消息（conversation.messages——来自 useConversation(id) 或 useConversations 详情）+ **流式增量**（chat.assistantText 附加在最后）
- chat.status === 'streaming' → 显示打字光标/状态消息；'error' → 错误条 + 重试提示

### 3. 新建会话
- `startConversation()`（mock）→ `useCreateConversation().mutateAsync({ title: '新会话', repositoryId: null })` → onSuccess 选中新会话 + invalidate lists
- createConversation 函数改造：async + onSuccess selectConversation(newId) + notify

### 4. 删除会话
- `deleteConversation(id)`（mock）→ `useDeleteConversation().mutate(id)` → onSuccess invalidate + 若删的是选中会话 → selectConversation(null)（或第一个剩余）

### 5. 组件结构调整
- ConversationPane 保持对外 props（onOpenFiles/onOpenOutputs/onOpenChanges）不变——AppShell 调用处不动
- 流式子组件 `StreamingChat`（新，文件内定义）：props { conversationId }——内部 useStreamChat + 输入框 + 消息流 + 打字机——ConversationPane 用 `<StreamingChat key={conversation?.id} conversationId={conversation?.id ?? ''} />`（key 切换会话重置）

### 6. 验证
- `npm run build` + `npm test`（App.test.tsx 的 workspace 测试——"selects and deletes conversations" 等——mock useConversations/useStreamChat 或调整）
- 浏览器实测（dev 4173 + 3003）：
  1. /workspace → 会话列表显示**真实会话**（主工程 DB，非 mock）
  2. 点会话 → 历史消息加载（GET 详情）
  3. 输入消息发送 → **真实流式打字机**（delta 逐字出现）→ done 后消息持久化
  4. 新建会话 → 创建 + 选中
  5. 删除会话 → 移除
  6. 无会话时发送 → 自动创建

## 不做
- 会话-项目（repositoryId）绑定 UI（后续 P 阶段——MVP 创建时 repositoryId null）
- OutputPane 的 conversations 依赖迁移（保持 useApp 兼容——P1-2 已完成产出面板接 REST？——检查 OutputPane 是否已独立；未独立则本任务只动 ConversationPane）

## 验收
1. build + test 全绿
2. 浏览器实测 6 项通过（真实流式打字机）
3. git commit + push

## 审查（流程铁律）
完成后调用 codex-review-and-fix skill 审查（手动模式）；报告 高/中/低 + 修复建议写 codex-review-p1-3c.md；修复后重新 build+test。
