# 微任务 P2-1b：前端 AgentsPage 接 REST

背景：P2-1a 已实现 Agent/Squad REST（GET/POST /agents + GET/PATCH /agents/{id} + GET /squads）。本任务：AgentsPage（src/pages/AgentsPage.tsx，当前用 initialAgents mock + useState 全量状态）迁移到真实后端。

## 范围（严格）

### 1. 数据层（参照 P1-4b repositories 模式）
- **src/types.ts**：AgentDto（id/name/role/kind/status/executionMode/tokenBudget/periodResetAt/createdAt/updatedAt——按 P2-1a agent-dto.ts 实际字段）+ SquadDto
- **src/api/agents.ts**：listAgents(filters?)/fetchAgent(id)/registerAgent(input)/updateAgent(id, patch)/listSquads——复用 client（unwrap:false 列表）
- **src/queries/agents.ts**：agentsKeys（lists/detail）+ useAgents/useAgent(id)（enabled 门控 + disabled key 工厂——参照 P1-4b）+ useRegisterAgent/useUpdateAgent mutations（onSuccess invalidate lists+detail）

### 2. AgentsPage 迁移
- **列表**：useState(initialAgents) → useAgents()（前端过滤 query/status/kind——同 P1-2b 模式）
- **详情**：selectedAgentId → useAgent(id)（或列表项直接渲染——**决定**：列表项渲染（不额外请求）+ 更新后 invalidate 刷新）
- **状态切换/执行模式切换**：现有 setAgentRows 本地改 → useUpdateAgent().mutate({ id, patch: { status } | { executionMode } }) → onSuccess invalidate + toast
- **注册表单**（页面若有"注册 Agent"）：→ useRegisterAgent（POST 幂等）→ onSuccess invalidate + 关闭
- **Squads tab**：useState(initialSquads) → useSquads？——**决定**：MVP 只做 Agents 列表/详情/操作真实化；Squads 列表用 useAgents 无——**新增 useSquads**（queries/agents.ts 内）——Squads 渲染真实数据（空数组显示空态）
- 移除：initialAgents/initialSquads 依赖（mock 数据仍可在 data/mock.ts 保留供其他页——只移除 AgentsPage 引用）
- 加载/错误/空态：isLoading/error/EmptyState

### 3. 验证
- npm run build + npm test（App.test.tsx 若挂 → vi.mock queries/agents 返回 mock agents——参照 repositories mock 模式）
- 浏览器实测（dev 4173 + 3003）：
  1. /agents → 真实 3 agents 列表（claude-code-1/coder-1/digital-1）
  2. 搜索/状态过滤前端生效
  3. 详情面板真实字段
  4. 状态切换（idle↔busy？——**注意**：用真实 agent 验证会改状态——切换后切回原状态）
  5. squads tab → 空态（真实 0）
- git commit + push

## 不做
- 凭证轮换 UI
- 注册表单若页面无则跳过（看页面结构）
- Agent 心跳展示

## 审查（流程铁律）
完成后调用 codex-review-and-fix skill 审查（手动模式）；报告写 codex-review-p2-1b.md；修复后重新 build+test。

**开工前先用 mcp__codegraph__codegraph_explore 探索**（查询 "AgentsPage initialAgents setAgentRows Agent executionMode status mock; queries/repositories useRepositories 模式; agent-dto" 等，项目根 C:\Users\13588\Desktop\AI_Coding_Center）。
