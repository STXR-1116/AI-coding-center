# CodingCenter 平台功能详述

## 一、平台定位与核心闭环

CodingCenter 是一个多 agent 协同的"编码中心"调度平台。物理员工通过助理 agent（数字人）接入平台，平台统一调度"需求 → 任务 → Agent 小队执行 → 结果回传"的全链路。

**核心用户**：普通员工（项目开发者）——体量最大的用户群体。其直接交互界面为"项目开发工作台"（详见 §15）：围绕具体项目工程，提供资源管理器、AI 会话、需求/任务产出面板、变更审查。

**核心闭环**：

```
用户（人）
  ↓ 口述/输入任务信息（工作台会话区，绑定项目）
数字人（交互层：Hermes 实例，提炼需求、自然语言反馈、流式对话）
  ↓ 提交为"需求"（自动提炼标题/描述 + 管理角色自动拆解任务）
Leader/PM（决策层：解析需求、拆解任务、派发、审批）
  ↓ 派发任务
Coder 小队（执行层：Claude Code/Codex 实例，开发、测试、回写结果）
  ↓ 回写
Leader/PM（验收）
  ↓
数字人（向用户反馈结果）
```

平台本身不直接写代码——这些是 agent 干的。平台做的是调度、管控、记忆、可观测、配置：把对的人/agent 在对的时机接到对的任务上，管住 token 算力消耗，记下全过程可追溯，并让一切可配置。

**Agent 大脑架构（重要区分）**：

- **数字人（DIGITAL）= Hermes/OpenClaw 实例**（对话/交互大脑）：理解用户描述、提炼需求、自然语言回复；即时 spawn（`hermes -z` / ACP 流式），非轮询
- **Coder/QA/Assistant = Claude Code/Codex 实例**（执行层）：connector 轮询拉取任务执行

---

## 二、角色体系与决策权边界

平台有两类参与者：**人**和 **agent**。

### 人的角色

| 角色 | 职责 |
| --- | --- |
| `employee`（物理员工） | 提交需求、查看自己需求及关联任务的结果 |
| `leader` | 执行调度：分配任务给 agent/小队、取消任务、管理 agent、管理全部配置 |
| `pm` | 规划：解析需求→任务拆解、查看全部需求/任务；不直接分配/取消任务（与 leader 分工） |

### agent 的角色

| agent kind | 定位 | 权限边界 |
| --- | --- | --- |
| 数字人（`digital`） | 交互层（Hermes 实例） | 理解用户描述 → 提炼需求（标题/描述）→ 代用户提交需求 + 自然语言回复 + 流式对话（ACP）；管理角色用户可自动拆解任务；不决策、不调度、不执行代码、不回写执行结果 |
| Coder（`coder`） | 开发与测试层 | 承接任务→执行→回写结果；scope 限定仅能操作自己承接的 task |
| QA（`qa`） | 质量闭环 | 质量验证；同 coder 的 scope 限定 |
| `assistant`（OpenClaw/Hermes） | 通用助理 | MVP 阶段单 agent 端到端打通（同时承担采集与执行）；阶段 4 拆分后职责归位 |

> **关键约束**：agent 以自身身份调接口时，scope 限定仅能操作自己承接的 `task_id`——防止 prompt injection 让 agent 越权操作别人的任务。

---

## 三、功能模块详述

### 1. 账号权限管理

**做什么**：管理平台所有人和 agent 的身份与权限。

- **用户管理**：用户 CRUD（创建/查/改/禁用，不物理删）；分配角色（employee/leader/pm）；改自己只能改姓名等非关键字段，改角色需 leader/pm；禁用用户（`status=disabled`）。
- **令牌管理**：短期 access 令牌 + 长期 refresh 令牌轮换；refresh 令牌哈希落库可撤销；登录签发、刷新轮换（撤销旧令牌+签新令牌）、登出撤销、登出全部设备（leader/pm 可强制下线他人）。
- **agent 凭证**：agent 有独立于用户账号的鉴权凭证（可轮换/撤销），用于 agent 以自身身份调平台接口。
- **权限矩阵**：操作 × 角色的权限表，鉴权在入口层做（是否登录/角色够不够），结果集过滤下沉 service 层（谁能看到哪些数据）。

### 2. 需求管理

**做什么**：从"用户想法"到"可执行需求"的承接与转化。

- **提交需求**：employee/leader/pm/digital（数字人代用户）可提交需求（标题+描述）；digital 代提交的需求可由 digital 修改（仅 draft 态）。聊天会话中数字人自动提炼需求（标题=语义提炼 ≤20 字，描述=完整上下文），管理角色用户自动拆解任务，非管理角色需求保持 draft（提示需管理员解析）。
- **需求可见性**：employee/digital 只看自己（代提交）的需求；leader/pm 看全部。
- **需求解析**：leader/pm 触发解析 → 调度核心把需求拆解为多个任务，并为每个任务估算 token 预算（基于 spec 复杂度 + 历史度量）。
- **需求取消**：提交者/leader/pm 可取消需求，级联取消关联任务。
- **需求状态机**：`draft → analyzing → in_progress → done / cancelled`。
- **需求模板库（P1-5）**：RequirementTemplate 模型 + 模板管理页（leader/pm）——预设需求结构（标题/描述/字段），新建需求时选模板预填（`{date}` 等占位符渲染）；把团队经验结构化为模板，提升需求质量。
- **Spec 版本化（P1-5）**：需求每次分析时对 Spec 生成不可变快照（RequirementSpec 版本历史），详情页可展开查看历史版本——需求变更可追溯。
- **上下文使用率（P1-6）**：任务估算/执行时计算上下文用量（CJK/ASCII 权重估算，0-100%），任务卡片显示"上下文 62%"徽章（>80% 红色告警）——防上下文溢出。

### 3. 任务管理与分发

**做什么**：需求拆解后的执行单元的全生命周期管理。

- **任务 CRUD + 可见性**：employee 看自己需求关联的任务；leader/pm 看全部；coder/qa 看自己承接的任务。任务可关联版本库（`repository_id`，可选）——详情页展示 Git 记录 + 文件浏览（见 §10）。
- **任务分配**：leader 把任务分配给单 agent 或小队（写 `assigned_agent_id` 或 `squad_id`）。
- **任务执行触发**：leader/pm 触发执行（经调度核心 + token 限流）；MVP 阶段由 leader 手动触发。
- **执行权限三模式（P0-3，TRAE 权限模式启发）**：`execution_mode = manual / auto / full`
  - `manual`（手动）：任务默认不被自动执行，需用户在任务详情点"执行"（含二次确认）；agent 拉取任务列表时过滤 manual 任务
  - `auto`（自动）：可自动执行；高危动作（如 git revert）仍走审批（`awaiting_approval` 态）
  - `full`（全权）：完全自主执行，不再审批
  - 配置入口：agent 编辑弹窗设默认值（`executionMode`）+ 任务详情可改（pending/assigned/failed 态）
  - manual 审批流：执行 → `awaiting_approval` → 批准（local 模式转 `assigned`+`auto` 供 connector 拉取；cloud 模式转 `running`+推送）→ 执行
- **任务取消**：leader/pm 取消，级联处理进行中的执行（→`cancelled`）。
- **结果回写**：agent 异步回写执行结果（output + status + token_used + model + 幂等键）；这是调度闭环的回写环。
- **结果读取**：用户读任务结果（可见性校验）。
- **任务状态机**：`pending → assigned → running → succeeded / failed / cancelled`；manual 模式增加 `awaiting_approval` 中间态。
- **token 预算灵活配置**（三方式 + 0=不限）：
  - 全局：PlatformConfig 配置（`tokenBudget.base` 等 4 参数），admin/token-budget 配置页可视化编辑
  - 单任务：任务详情 tokenBudget 编辑（pending/assigned/failed 态）
  - agent 周期：agent 编辑弹窗 `tokenBudgetPeriod`
  - `0 = 不限`（task 级与 agent 级均支持）

### 4. 调度核心

**做什么**：平台的中枢——需求解析、任务拆解、派发、token 限流。

- **需求解析→任务拆解**：把一个需求拆成多个可执行任务，每个任务带 spec + token 预算估算。
- **派发**：把任务派发给 agent（HTTP 调用，MVP）；按运行时模式派发到对应 runtime（本地 connector pull / 云端直起）。
- **token 限流（三层）**：
  - **task 级预算**：每个任务有 `token_budget`，预扣；执行消耗；失败/取消时未消耗额度退回。
  - **agent 级周期预算**：每个 agent 有周期预算（`token_budget_period` + 已用 `token_used_period`），防止单 agent 在周期内耗尽算力。
  - **流式增量累计**：流式 LLM 调用时每 N tokens 检查预算，超额中断流 + 标 `failed(over_budget)`。
- **agent 通信**：调度核心承担 agent 通信（HTTP，MVP）；未来可替换为 MCP adapter（接入外部/异构 agent）。

### 5. Agent 配置

**做什么**：管理所有 agent 实例（OpenClaw/Hermes/数字人/Coder/QA 统一建模，kind 字段区分）。

- **agent 注册/注销**：leader/pm 注册 agent + 签发凭证；注销时撤销凭证。
- **agent 配置**：改 config/状态/运行时模式。
- **agent 状态机**：`idle / busy / offline / stale`（心跳超时自动转 `stale`，供 reclaim）。
- **心跳机制**：agent 定期心跳，更新 `last_heartbeat_at`；busy 超时阈值自动转 `stale`/`offline` + 任务执行超时 reclaim（防 zombie agent——崩溃后永久 busy）。
- **agent 可见性**：employee 只看 assistant 类（数字人/通用助理）；leader/pm 看全部。

### 6. Agent 运行时双模式 + 数字人大脑

**做什么**：agent 能力接入有本地 + 云端两种模式；数字人（DIGITAL）为 Hermes/OpenClaw 实例。

- **本地模式**：平台后端通过本机 connector 中转调用用户本机 CLI（Claude Code/Codex）。用户本机跑轻量 connector daemon，后端经轮询拉取下发任务，connector 调起本机 CLI 执行后回传结果。
  - connector 注册（上报 `cli_kind` + 版本/路径等环境信息）、心跳、拉取待执行任务、回写结果。
  - connector 离线时本地任务挂起 + UI 告警 + 可手动回退云端模式。
  - NAT/防火墙致不通时回退 connector 主动 pull（轮询）模式。
- **云端模式**：平台在云端服务器直接启动 agent 实例，管控完整生命周期。
- **模式路由**：用户在 UI 上切换本地/云端，不按任务自动路由（简化设计——模式是会话级开关）。
- **双模式 token 计量对齐**：本地 CLI 的 token 消耗由 connector 解析 CLI 输出上报，与云端直调 LLM 的计量口径对齐到同一字段 + 按模型归一。
- **connector 运维可见**：leader/pm 可查 connector 列表（含 online/offline/stale 状态过滤）+ 单个详情，感知 stale connector。
- **数字人大脑**（DIGITAL = Hermes/OpenClaw 实例）：聊天场景即时 spawn Hermes CLI（非 connector 轮询）
  - **oneshot 模式**：`hermes -z "<prompt>" --cli` —— 单次执行，返回结构化 JSON（reply/title/description），失败回退规则提炼
  - **ACP 流式模式（默认）**：`hermes acp` 常驻进程（JSON-RPC over stdio）——`initialize` → `session/new` → `session/prompt` → `agent_message_chunk` 事件流式推送 → 前端打字机逐字显示（首个字 ~7s，对比 oneshot 20s+）；回复末尾 `---JSON---` 行携带提炼的 `{title, description}`
  - **链路**：前端 `fetch /api/chat/stream`（NDJSON）→ 后端 ACP client → Hermes；失败逐级回退（ACP → oneshot → 模板）
  - **数字人对话上下文**：注入会话历史（最近 6 条）+ 当前消息 + 提炼指令

### 7. Agent 小队（squad）

**做什么**：Coder+QA 协同的载体。

- **小队 CRUD**：创建小队（`name` + `lead_agent_id`）、查、改、解散（不物理删）。
- **成员管理**：加/移除成员，每个成员带角色（coder/qa/lead）。
- **任务分配到小队**：把任务分配给整个小队（写 `squad_id` + `task_agents` 关联）。
- **小队可见性**：employee 看自己需求关联的小队；leader/pm 看全部。

### 8. 知识库挂载（= ContextDB）

**做什么**：知识库就是 ContextDB，全外挂 MCP server，平台不自存记忆/向量。

平台只做三件事：

1. 记录"哪个 agent 绑定哪个知识库 MCP server" + 访问凭证；
2. 运行时把绑定的 MCP server 配置注入 agent 上下文，agent 自行经 MCP 协议检索/读写；
3. 预留用户可配置的交互入口（检索参数、召回数量、相似度阈值等存为可配置项，平台透传给 agent，不解释语义）。

- **知识库 CRUD**：注册（`mcp_server_url` + name + 凭证 + config）、查、改、删（禁用而非物理删）。
- **agent 绑定**：多对多绑定（一个 agent 可绑多个知识库，一个知识库可被多 agent 复用）。
- **"进化"是知识库自己的功能**：被动召回 / 主动学习 / 混合检索都由 MCP server 实现，平台不开发、不存相关数据；平台只预留配置入口。
- **连通性保障**：注册时探测 MCP server 可达 + 凭证有效；定期健康检查；不可达时 UI 告警 + agent 检索降级（跳过该知识库 + 提示）。
- **凭证安全**：凭证独立存储（不入 config jsonb），注入 agent 上下文时不回显，审计检索调用。

### 9. Skill 技能管理

**做什么**：Skill 是可复用的能力包（一份能力描述 + 可选脚本/资源），agent 执行任务时按需调用。

- **Skill CRUD**：创建/上传（manifest + 脚本存储）、查、改 manifest/版本、废弃（deprecated，不物理删，可回滚）。
- **版本管理**：Skill 有版本，版本固定（防偷偷改）。
- **agent 绑定**：多对多绑定（一个 agent 可绑多个 skill，一个 skill 可被多 agent 复用）。
- **任务指定 Skill**：任务分发时选定用哪些 skill（任务-Skill 关联）。
- **注入 agent 上下文**：任务指定 skill 后，把 skill manifest + 脚本注入 agent 执行上下文，agent 能调用该 skill。
- **安全**：Skill 脚本在沙箱/受限环境执行 + 作者审计 + 版本固定。

### 10. 版本库（SVN/GIT）+ repo-preview

**做什么**：代码产出托管 + 任务内"代码上下文/结果审查"（参考 VS Code 源码管理）。

- **仓库配置**：leader/pm 配置远端仓库（vcs 类型 git/svn + url + 凭证 + `default_branch`）；支持本地路径（`localPath`，注册时填本地仓库路径——用于任务内 Git 记录/文件浏览，本地路径与远端 URL 双模式）。仓库可归属用户（`owner_user_id`）或 agent（`owner_agent_id`，预留，MVP 不用）。
- **任务拉取上下文**：配置任务从版本库拉代码作上下文（`repository_id` + branch + path）。创建任务可选"关联版本库"（下拉选 active 仓库）。
- **代码产出管理**：agent 产出代码 → 存为 `code_artifact`（产出内容引用，不进 DB）→ 推送到远端 → 回写 `commit_hash` + `push_status`。
- **推送**：leader/pm/agent（自己产出）推送产出到远端（用 svn/git CLI）；推送前二次确认目标分支；`push_status` 全程留痕 + 审计。
- **推送失败处理**：`push_status=failed` + `push_error` 落库 + 任务可重试 + 不污染远端（失败回滚本地提交）。
- **大产出**：分块推送 + 超限告警。
- **可见性**：coder/qa 仅看自己产出（按 agent_id 过滤）。
- **repo-preview**（任务详情内，参考 VS Code）：
  - Git 记录（提交日志图）：`git log --graph` 解析 → SVG 分支线/节点/refs 徽章（master/HEAD/origin）+ 相对时间 + 提交消息 + 作者；VS Code 源码管理风格；hover 气泡展示完整提交正文（body）；分页加载（30 条/页 + 加载更多）
  - 文件浏览：文件目录树（深度 ≤3、每目录 ≤500、排除 `node_modules`/`.next`/`.git`）+ 代码预览（≤512KB、二进制拒绝）
  - 回滚：每行提交"回滚"按钮（`git revert` 保留历史，非 reset）+ 二次确认 + `VCS_REVERT` 权限（leader/admin）+ confirm 弹窗
  - 安全：`resolveSafePath` 穿越防护（startsWith repoPath+sep）、`execFile` 参数化无 shell、hash 白名单 `^[0-9a-f]{7,40}$`

### 11. 记忆与可观测

**做什么**：平台的"眼睛和账本"——审计、度量、备份（记忆已外挂知识库，平台不自存）。

- **审计（旁路）**：记录全操作（create/update/delete/assign/execute/analyze/cancel/login/logout/token_revoke），actor 区分 user/agent；审计写入失败不回滚业务（旁路，仅记日志）；leader/pm 可查询（过滤 + 分页）。
- **度量**：MVP 落三类指标（token 消耗 / 执行时长 / 成功率）；agent/service 旁路上报；leader/pm 查询聚合。
- **备份**：数据库定时备份 + 保留策略 + 备份元数据记录（开始时间/大小/位置/状态/校验时间）。

### 12. 平台可配置性

**做什么**：七项能力在 UI 上均可配置，即时生效，无审批。

- **模块开关（粗粒度）**：七项能力（知识库/Skill/任务分发/Agent配置/版本库/账号权限/Dashboard）每一项可整模块启用/禁用。禁用 → 该模块接口被入口层中间件统一拦截（`423 Locked`）+ 前端隐藏入口 + agent 注入跳过；启用后立即恢复。
- **参数可配（细粒度）**：各能力实体的具体参数走已有 CRUD 接口（知识库填 MCP server URL、版本库填仓库地址、Agent 改 config 等）—— CRUD 即配置入口。
- **即时生效**：配置/开关改动写库后立即生效，下次 agent 调用或接口请求即读新值，无审批流程、无需重启。读多写少缓存 + 写时失效。
- **配置权限**：模块开关仅 leader/pm 可改；实体参数配置权限随各域既有鉴权。
- **批量开关**：前端串行调用单条 PUT（逐条即时生效，短暂不一致 ≤1s 可接受）。
- **禁用影响范围**：前端静态映射（模块→影响描述），不另设 impact 接口；禁用核心模块（如 `task_dispatch`）会中断调度，开关改动二次确认 + 审计 + UI 显式告警影响范围。

### 13. Dashboard（仪表盘）

**做什么**：统计聚合 + 可视化 + 管理页。

- 统计聚合（任务/执行/agent/token 消耗等多维度）。
- 仪表盘可视化。
- 管理页（agent/用户/配置等集中管理）。

### 14. 用户 × 数字人聊天（已实现）

**做什么**：普通员工与数字人（Hermes 实例）对话交互——描述任务、接收反馈；数字人提炼需求/任务，页面展示当前会话相关需求/任务，可点击跳转需求/任务详情。

- **会话管理**：Conversation 模型（归属用户/绑定 DIGITAL agent/自动标题=首条消息前 20 字）；左侧会话列表（标题 + 相对时间 + 删除）；新建会话；历史会话切换。
- **消息**：ChatMessage（role: user/agent + entities JSON）；user 右对齐蓝气泡 / agent 左对齐 surface 气泡（markdown 渲染，react-markdown + md-prose 样式）。
- **数字人回复**（Hermes 实例，流式）：ACP 常驻进程流式（打字机逐字显示）→ 失败回退 oneshot → 模板；回复末尾 `---JSON---` 行携带提炼 `{title, description}`。
- **需求/任务提炼**：数字人理解描述 → 提炼标题（≤20 字）/描述 → `createRequirement`（user 身份）→ 管理角色自动 `analyzeRequirement` 拆任务 → 回复含实体卡片（📋 需求 / ✅ 任务，点击跳转 `/requirements/[id]` / `/tasks/[id]`）；闲聊（`title=null`）不建需求，纯对话。
- **实体卡片**：entities 结构化 JSON → 前端渲染卡片（类型徽章 + 标题 + "查看"）→ 跳转详情页。
- **流式传输**：`/api/chat/stream`（NDJSON）→ ReadableStream 逐块推前端打字机；首个字 ~7s；完成后 done 帧携带 agentMessage + entities。
- **回退链路**：ACP 不可用 → oneshot（`hermes -z`）→ 模板回复（保底可用）。
- **安全**：会话严格归属隔离（他人会话 FORBIDDEN）；需求/任务创建旁路容错（失败不中断会话，console.error 日志）。

### 15. 项目开发工作台（设计定稿，待实施）

**做什么**：普通员工（项目开发者）的主交互界面——从"聊天页"升级为"项目开发工作台"。参照 VS Code Workbench 形态（浏览器内实现），核心是"**会话绑定项目**"。

**信息架构（三栏 + 底部抽屉）**：

- **顶栏**：项目切换器（VcsRepo 下拉）+ Git 分支/状态徽章 + 会话标签 + 用户
- **主侧边栏**（w-56）：资源管理器——文件树 / 代码预览 / Git 记录（复用 repo-explorer 能力，切换 tab）
- **编辑区**（中间）：AI 会话区（数字人聊天，多会话标签借鉴 Cursor Agent Tabs + 流式 + 实体卡片）
- **产出面板**（w-72，差异化核心）：当前会话的需求列表 + 任务列表（状态徽章、点击跳转详情、agent 执行进度）
- **底部抽屉**：变更审查 diff（本会话 agent 修改的文件 + 内容对比，新增绿/删除红 + 接受/拒绝；拒绝 = `git revert` 复用 `VCS_REVERT`）

**核心交互流（黄金路径）**：

```
打开工作台 → 选项目 → 描述任务 → 数字人流式回复 + 提炼需求（产出面板出现需求卡片）
→ 拆任务（任务列表 + 状态流转）→ agent 执行 → 底部 diff 抽屉出现变更
→ 审查接受/拒绝 → 验收
```

**数据模型扩展**：

- `Conversation.repositoryId`（会话绑定项目，null=通用会话；新建会话选项目或"在此项目发起会话"）
- 产出面板零新表：从 `ChatMessage.entities` 聚合当前会话需求/任务
- `TaskChange` 表（taskId/repositoryId/filePath/diff/status，P2 可选）：MVP diff 实时查 git（任务 commit 前后），TaskChange 留历史/审批状态

**分阶段**：

- **P1** 骨架 + 项目绑定（`/workspace` 三栏布局 + repositoryId + 资源管理器集成 + 产出面板 v1 从 entities 聚合）
- **P2** 变更审查 diff 抽屉（任务完成 → 变更列表 + diff 视图 + 接受/拒绝）
- **P3** 增强（TaskChange 落库 / 会话标签 / 数字人注入项目上下文 / agent 执行过程实时状态）

---

## 四、横切机制（贯穿所有模块）

### 1. 幂等与一致性

- 所有 agent 异步回写点带幂等键（`idempotency_key`），防 agent 回调重复/乱序写入（双重计数）。
- agent 状态机（idle→busy）+ 数据库行锁/乐观锁防并发竞态（多任务抢同一 agent）；try/catch 转冲突 409。

### 2. 安全

- agent 凭证独立存储，不入 config jsonb；定期轮换 + 审计密钥访问。
- agent scope 限定：仅能操作自己承接的 `task_id`，防 prompt injection 越权；敏感操作二次校验；LLM 输出与 API 调用解耦。
- 知识库/版本库凭证：独立存储，注入不回显，审计调用。

### 3. 可观测性

- 审计旁路（写入失败不回滚业务）+ 缺失告警。
- 度量采集（token/时长/成功率）。
- connector/知识库 MCP server 健康检查 + stale/offline 检测。

### 4. 可替换实现抽象

平台的基础设施（存储 / LLM / 调度队列 / agent 通信协议 / agent 运行时 / 部署）都定义接口，MVP 给能跑的实现，配置开关切换——不"预留"不实现，至少给一个能跑的实现，接口才有真实约束力。扩展点是同一接口的另一个实现（如调度队列 MVP 用 BackgroundTasks，未来可换 Redis Stream；agent 通信 MVP 用 HTTP，未来可换 MCP adapter）。

### 5. 契约层单一真相源

所有接口定义、schema、业务类型、枚举集中在契约层，前后端 + agent 执行器都依赖它。接口变更先改契约层，类型检查即强制两端同步。
