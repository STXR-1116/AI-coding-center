# AI_Coding_Center 补全计划（接入 Coding Center 主工程）

> 更新：2026-08-06 | 状态：规划定稿，P0 启动
> 目标：把 AI_Coding_Center（高保真前端原型，100% mock）接入 Coding Center 主工程真实后端，按 README 契约（743 行）实现完整功能闭环。

## 一、现状（勘察结论 2026-08-06）

- **AI_Coding_Center**：React 19 + Vite 7 + React Router 7 + TanStack Query 5（空挂载）+ Recharts（未用）。9 页面（tasks/workspace/requirements/agents/repositories/knowledge/skills/analytics/settings）交互完成度高，但**零网络请求**、数据全 mock、无登录、无 capabilities 权限模型。分析报告：`analysis-report.md`（26 项差距 P0-P3）。
- **Coding Center 主工程**：Next.js 14 全栈（3003），需求/任务/Agent/小队/知识库/技能/版本库/聊天/工作台/配置全功能 + 148 测试。后端为 **server actions + service**（非 REST）。
- **README 契约**：`/api/v1` REST + camelCase + UUID/UTC + Bearer + Idempotency-Key + version 乐观锁 + capabilities/allowedActions 权限 + NDJSON 流式聊天 + WS/SSE 实时事件 + 统一 error envelope。

## 二、架构决策

| 决策 | 结论 | 理由 |
| --- | --- | --- |
| 后端形态 | **主工程（3003）实现契约 REST API**（Next.js App Router route handlers 在 `/api/v1` 下） | 一套部署；主工程 service 层复用；契约=单一真相源（README 要求） |
| 前端接入 | AI_Coding_Center `VITE_API_BASE_URL=http://127.0.0.1:3003/api/v1`（dev 4173 + vite proxy / CORS） | README 环境变量已预留 |
| 鉴权 | 主工程复用现有 NextAuth 凭证登录，新增 token 端点；refresh 用 HttpOnly cookie | 契约 §鉴权 |
| 数据迁移 | 主工程 service 之上包 REST 适配层（DTO 映射 camelCase + capabilities/allowedActions 计算） | 不重写 service，只加接口层 |
| 权限真相源 | 主工程计算 capabilities/allowedActions 返回；前端只用它控制 UI | 契约明确要求 |

## 三、阶段拆分（每阶段含微任务，验收 = 测试 + 浏览器实测 + commit）

> 状态标注：✅ 完成（2026-08-07）｜◐ 部分（差异说明）｜⬜ 未开始。实际实现与计划有偏差处以「实」标注。

### P0 — 地基（全部完成 ✅）

| 微任务 | 内容 | 验收 |
| --- | --- | --- |
| P0-1 主工程 REST 骨架 ✅ | `/api/v1` route handlers 基础设施：统一 error envelope（code/message/details/requestId/retryable）、Idempotency-Key middleware（actor+method+path，24h，payload 哈希）、version 乐观锁（409 VERSION_CONFLICT）、JSON 序列化 camelCase | curl 验证 error/幂等/409；单测（实：乐观锁并入状态机 409——状态冲突 STATE_CONFLICT） |
| P0-2 鉴权端点 ✅ | `POST /auth/login` `POST /auth/refresh` `POST /auth/logout` `GET /auth/me`（user/role/capabilities/可见模块/默认项目）；复用现有用户体系，签发短期 access token + refresh cookie；**capabilities 计算器**（现有 hasCapability 扩展为清单） | curl login→me 闭环；capabilities 与前端按钮映射表单测 |
| P0-3 前端 API 层 ✅ | `src/api/client.ts`（Bearer + 401 refresh 重试 + 幂等键 + 错误解析 + version 头）+ `src/api/*.ts` 领域函数 + `src/queries/*.ts`（query keys/mutations）+ 登录页（新路由 /login）+ auth/me 初始化 | 前端可登录并渲染 me；React Query 首次 useQuery 生效 |
| P0-4 权限模型改造 ✅ | 删除 role 硬判；按钮由 capabilities/allowedActions 驱动（hook：useCapability/useAllowedActions）；SettingsPage/TasksPage/WorkspacePage 改造 | 浏览器实测按钮按能力显隐；单测 |

### P1 — 核心业务闭环（全部完成 ✅）

| 微任务 | 内容 | 验收 |
| --- | --- | --- |
| P1-1 任务域 REST ✅ | `GET/POST /tasks`、`GET/PATCH /tasks/{id}`、`/assign`、`/execute`、`/retry`、`/cancel`（映射现有 task service + 状态机 + 审批创建）；`contextUsage` 计算 | curl 全链路（创建→分配→执行→取消→重试）；单测 290 绿 |
| P1-2 前端任务页接 API ✅ | TasksPage：列表（cursor 分页）/详情/事件时间线/审批/执行/取消 mutation + query 缓存；CreateTaskDialog 接 REST（需求下拉） | 浏览器实测真实任务流；Vitest 57 绿 |
| P1-3 会话 + NDJSON 流式 ✅ | `GET/POST /conversations`、`/{id}`、`/messages`、`DELETE`；前端 `src/api/stream.ts` NDJSON 逐行解析 + useStreamChat 打字机 + WorkspacePage 会话面板真实化（实：直接复用主工程 `/api/chat/stream` NDJSON 通道，不重复实现流式端点） | curl NDJSON 全帧；浏览器实测打字机流式 |
| P1-4 工作台接 API ✅ | WorkspacePage：会话 tabs 真实化、Git 记录（useCommits）、变更审查抽屉（useChanges + revert 按钮）、RepositoriesPage 仓库列表/详情/提交（实：文件树保持 mock——主工程无文件树端点，标注 P2；产出面板聚合 P2） | 浏览器实测工作台全链路 |
| P1-5 变更审查 ✅ | changes/revert 端点（实：并入 P1-4a 仓库 REST——`GET /repositories/{id}/changes` + `POST /changes/revert`，VCS_REVERT 能力校验） | curl + 浏览器实测 |
| P1-6 需求分析 ✅ | `POST /requirements`、`POST /{id}/analyze`、`GET /{id}/specs`、`POST /{id}/cancel`；前端 RequirementsPage 全接（创建/解析/版本化/取消）（实：analyze 为同步实现——draft→in_progress + spec v1 生成，非 202 异步） | curl 异步闭环（实：同步闭环）；浏览器实测 AI 分析生成 Spec |
| P1-7 实时事件层 ✅ | 主工程 SSE（实：`GET /api/v1/events`——EventBus + keep-alive + task.status_changed 广播；Last-Event-ID/RBAC 过滤留待增强）；前端 `useRealtimeEvents`（EventSource 订阅 + query 失效） | 双端实测（curl 推事件 → 浏览器自动重拉） |

### P2 — 管理页（全部完成 ✅ 2026-08-07）

| 微任务 | 内容 |
| --- | --- |
| P2-1 Agent/Squad REST + 前端 ✅ | `/agents`（列表/详情/注册/PATCH 三分派：executionMode/tokenBudget/status——审查修复：错误映射器/M2 负数预算/M3 预检前置）、`/squads`；AgentsPage 接 REST（真实 3 agents + 状态/模式切换 + 自改提示） |
| P2-2 知识库/Skill REST + 前端 ✅ | `/knowledge-bases`（7 端点含 bind/unbind/disable）、`/skills`（8 端点含 deprecate/reactivate）；KnowledgePage + SkillsPage 接 REST（注册验证拦截 + 绑定开关真实化——审查修复：高-1 详情绑定态） |
| P2-3 仓库注册 + 配置 PUT ✅ | `/repositories/{id}/test`（探活）、`/config/token-budget`（版本化 PUT + 409 VERSION_CONFLICT——审查修复：H1 TOCTOU 事务 CAS）、`/backups`（列表/创建/verify）；SettingsPage 预算 tab + RepositoriesPage 注册/测试接 REST（审查修复：H1 旧字段契约对齐） |
| P2-4 Dashboard/审计 + 用户管理页 ✅ | `/dashboard/summary`、`/metrics/summary`、`/audit-logs`、`/users`（列表/PATCH 三分派 + 自改保护 403——审查修复：M1 404 预检）；AnalyticsPage Recharts 真实化 + **新增 UsersPage**（用户管理——审查修复：H1 metrics key 稳定） |

### P3 — 增强（全部完成 ✅ 2026-08-08）

| 微任务 | 内容 |
| --- | --- |
| P3-1 分页 ✅ | useInfiniteTasks + 加载更多（20 → 全部实测）；requirements/audit 本地展开（数据量小，避免单页/无限缓存错配） |
| P3-2 错误统一 ✅ | parseApiError 结构化 + 401/403/VERSION_CONFLICT 特判（STATE_CONFLICT 透传不误伤）+ 409 自动刷新 + 7 测试 |
| P3-3 API 文档 ✅ | 主工程 API.md（51 路由/15 域/鉴权/幂等/已知局限——实际实现为真相源）+ README 引用 |
| P3-4 模块开关 REST ✅ | GET/PATCH /modules（listToggles 包装 + 21 测试）；SettingsPage 模块开关 mock → REST + AppContext 镜像保导航联动 |
| P3-5 Agent 运行时 ✅ | scripts/agent-runner.mjs（心跳/pull/start/DeepSeek 执行/results 回传）+ TaskDto.result 透传 + 前端查看结果 Dialog；评审修复（libuv 优雅退出/prompt 注入隔离/真实 token 计数） |
| P3-6 runner 增强 ✅ | EXECUTOR=claude（CC CLI 真实编码 + deepseek 兜底）+ 配置参数化 + result markdown 渲染（react-markdown） |
| P3-7 服务化 ✅ | pm2 托管（autorestart/日志轮转/内存上限/pm2 save）+ 手动进程防双实例 |
| P3-8 审批链路 ✅ | approval.created/resolved SSE 广播 + 前端映射 invalidate；POST /tasks/{id}/approve 放行端点（12 测试）+ 前端批准按钮接真实端点（修复 mock 时代死链） |
| P3-9 平台参数 ✅ | GET/PUT /config/platform（整块 JSON 版本化 + TOCTOU $transaction 防护 + 13 测试）；SettingsPage 平台参数 mock → REST（409 重拉）；审查修复 payload.version 污染 |

**遗留**：平台参数消费方（defaultExecutionMode 应用任务创建——后续）；agent.health SSE 广播（预留）；知识库 MCP 实际接入（注册校验已有，消费待真实 MCP server）。

## 四、执行约束（PM 职责）

- **PM 只做**：读计划、查规范（README 契约/主工程 service）、发任务（CC）、验收（测试+浏览器）、更新计划。产品代码/UI 一律 CC 执行。
- CC 派活：新会话 + max-turns 适当 + `.pm-task-*.md` 任务书传参；失败重试/拆微任务；包月不计预算。
- 验收铁律：vitest 全绿 + build 通过 + curl/浏览器实测，CC 声称完成必须验证。
- 文档更新：只用 patch 增量；开发计划 checkbox 如实打勾。

## 五、风险

- 契约与主工程现有模型差异（camelCase/UUID/displayId/软删除）——P0-1 定 DTO 映射规范先行。
- 主工程 server actions 与 REST 并存——REST 层只读 service，不双写业务逻辑。
- 大工程 CC 反复失败——按微任务粒度（每任务 ≤1 文件组）派发。
