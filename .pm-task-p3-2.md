# 微任务 P3-2：error.code 统一处理（409 重拉 / 401 / 403 特判）

背景：P3-1 完成。P3 增强：handleApiError（src/queries/errors.ts）只转 message——丢失 code/retryable 语义。本任务：错误信息结构化 + 特判文案 + 409 重拉钩子。

## 范围（严格）

### 1. errors.ts 增强
- **parseApiError(error): ApiErrorInfo**（{ message, code?, retryable? }——ApiClientError 有 code/retryable 字段；非 ApiClientError → { message: '操作失败' }）
- **handleApiError 保留**（兼容现有调用）——内部走 parseApiError，但加特判文案：
  - 401 UNAUTHENTICATED → "登录已过期，请重新登录"
  - 403 FORBIDDEN → "没有执行此操作的权限"
  - 409（VERSION_CONFLICT/STATE_CONFLICT）→ "数据已被其他操作修改，请刷新后重试"
  - 其他 → error.message

### 2. useConflictRefetch 钩子（src/queries/errors.ts 或 hooks/）
- 使用场景：mutation onError 收到 409 → 自动 refetch 相关 query（列表/详情刷新到最新）
- `useConflictRefetch()` 返回 { refetchOnConflict(error, keys?) }——内部判 code 是否 409 → queryClient.invalidateQueries(keys ?? 'all') + 返回 true（调用方决定是否 toast）
- **接线**：SettingsPage 预算保存已有 409 处理（VersionConflictError）——本钩子用于其他 mutation（agents/knowledge/skills/users PATCH——409 时自动刷新列表）——**MVP**：在 users PATCH（useUpdateUser onError）+ agents PATCH 接上（409 → invalidate + info toast）

### 3. 验证
- npm run build + npm test（新增 errors.test.ts：parseApiError/handleApiError 特判文案——4-5 用例）
- 浏览器实测：users PATCH 触发 409（两浏览器同时改同用户 role——难模拟——**改验证**：单测覆盖 409 分支 + 手动 curl 验证 409 响应）
- git commit + push

## 不做
- 全局错误边界（页面级 ErrorBoundary 已有）
- 401 自动登出跳转（RequireAuth 已处理）
- 每个 mutation 都接（MVP 只接 users/agents PATCH）

## 审查（流程铁律）
完成后调用 codex-review-and-fix skill 审查（手动模式）；报告写 codex-review-p3-2.md；修复后重新 build+test。

**开工前先用 mcp__codegraph__codegraph_explore 探索**（查询 "handleApiError errors.ts ApiClientError code retryable; useUpdateUser onError; useUpdateAgent" 等，项目根 C:\Users\13588\Desktop\AI_Coding_Center）。
