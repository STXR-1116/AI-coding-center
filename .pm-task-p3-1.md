# 微任务 P3-1：列表分页增强（useInfiniteQuery 加载更多）

背景：P1/P2 完成。P3 增强：当前列表页（tasks/requirements/audit）只拉第一页（后端 pageEnvelope 有 nextCursor/hasMore 但前端未消费）。本任务：三个列表页接入"加载更多"。

## 范围（严格）

### 1. 查询层改造（src/queries/）
- **tasks.ts**：useTasks 保持 useQuery（列表搜索/筛选场景）——**新增 useInfiniteTasks()**（useInfiniteQuery：getNextPageParam = lastPage.page.nextCursor；返回 pages 合并）
- **requirements.ts**：新增 useInfiniteRequirements()（同模式）
- **dashboard.ts**：useAuditLogs 改造为 useInfiniteAuditLogs（同模式）——或保留 + 新增（**决定**：audit 直接改造为 infinite——审计是唯一分页消费场景）
- **api 层**：listTasks/listRequirements/fetchAuditLogs 已返回 page envelope（unwrap:false）——确认参数透传 cursor（看现有 params 是否含 cursor——没有则补）

### 2. 页面接入
- **TasksPage**：列表底部"加载更多"按钮（hasMore 时显示；isFetchingNextPage disabled；无更多隐藏）——数据源改 pages 合并数组
- **RequirementsPage**：同模式
- **AnalyticsPage 审计 tab**：现有"加载更多"按钮（已有点击逻辑）→ 接 useInfiniteAuditLogs（真实追加）

### 3. 验证
- npm run build + npm test（App.test 若挂 → 更新 tasks/requirements/dashboard mock 返回 page envelope 形状或 pages 形状——**注意**：mock 用模块级稳定引用）
- 浏览器实测：
  1. /tasks 滚动到底 → 加载更多 → 列表变长（27 条 → 更多？——后端有 28+ 任务——分页 limit 默认 20 → 第一页 20 + 加载更多 8）
  2. /analytics 审计 tab → 加载更多 → 追加 10 条
- git commit + push

## 不做
- 虚拟滚动（数据量小）
- 无限滚动（按钮足够）
- 后端分页参数改动（已支持）

## 审查（流程铁律）
完成后调用 codex-review-and-fix skill 审查（手动模式）；报告写 codex-review-p3-1.md；修复后重新 build+test。

**开工前先用 mcp__codegraph__codegraph_explore 探索**（查询 "useTasks tasksKeys query key 工厂; TasksPage 列表渲染 状态筛选; listTasks page nextCursor; useInfiniteQuery" 等，项目根 C:\Users\13588\Desktop\AI_Coding_Center）。
