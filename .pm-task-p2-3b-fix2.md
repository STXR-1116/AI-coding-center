# 微任务 P2-3b-fix2：审查修复（H1/M1/M2/M3/M4）

背景：codex-review-p2-3b.md 审查发现 1 高 4 中 2 低。修 H1（必须）+ M1-M4（AI_Coding_Center src/）。

## H1（必须）：TokenBudgetConfig 残留旧字段
- src/types.ts:725 TokenBudgetConfig 仍含 monthlyTokenBudget/singleTaskTokenLimit/budgetWarningThreshold——删掉（只留 base/per100Chars/min/max + version）
- 检查 SettingsPage 是否还有旧字段引用（全文件 grep monthlyTokenBudget/singleTaskTokenLimit/budgetWarningThreshold——应全部清空）

## M1：PUT 幂等注释与实现不符
- src/api/config.ts（或 client）：updateTokenBudgetConfig 注释声称幂等但 Idempotency-Key 只对 POST 生效——改注释：乐观锁 version 提供并发保护（PUT 无幂等键）

## M2：测试盲区
- src/App.test.tsx：加 vi.mock('./queries/config')（useTokenBudgetConfig 返回 mock 配置 + useUpdateTokenBudgetConfig）——repositories mock 补 useRegisterRepository/useTestRepository（返回 mutate spy）

## M3：latencyMs 死代码
- src/pages/RepositoriesPage.tsx:229：`r.latencyMs ?? '?'` ——类型是必填 number → 改直接用 r.latencyMs（或类型改为可选——看 api/repositories.ts testRepository 返回类型——若后端可能缺则类型改可选）

## M4：注册表单 description/language 丢弃
- RepositoriesPage handleConnect：description 字段——RegisterRepositoryInput 无 description → 看主工程 POST /repositories 是否接受 description（P1-4a route——**决定**：若后端不支持则表单删 description/language 输入框或标注忽略——不静默丢弃：删输入框）

## 验收
1. npm run build + npm test 全绿
2. git commit + push
