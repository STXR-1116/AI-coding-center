# 微任务 P2-3b：SettingsPage 预算配置 + RepositoriesPage 注册/测试接 REST

背景：P2-3a 已实现 config/token-budget（版本化 PUT）+ repositories test + backups REST。本任务：前端 SettingsPage 运行与预算 tab 接真实配置 + RepositoriesPage 注册表单/测试连接接 REST。

## 范围（严格）

### 1. 数据层
- **src/api/config.ts**：fetchTokenBudgetConfig()（GET）/ updateTokenBudgetConfig(input: { version: number } & TokenBudgetConfig)（PUT）——参照 api/agents.ts 模式
- **src/queries/config.ts**：configKeys + useTokenBudgetConfig（query）/ useUpdateTokenBudgetConfig（mutation——onError 409 VERSION_CONFLICT 时特殊处理：重新 fetch + 提示"配置已被他人修改，已刷新最新值"）
- **src/types.ts**：TokenBudgetConfig（base/per100Chars/min/max——按 P2-3a config-dto 实际字段）
- **src/api/repositories.ts 补**：testRepository(id)（POST /repositories/{id}/test）→ { ok, latencyMs, message }

### 2. SettingsPage 运行与预算 tab
- defaultConfig mock → useTokenBudgetConfig（loading 骨架 + error 态）
- 保存按钮：useUpdateTokenBudgetConfig().mutateAsync({ version, ...formValues }) → 成功 toast"已保存"；409 → 自动重拉最新值 + info toast
- 保存后 version 状态同步（mutation 返回新 version）
- 移除 defaultConfig mock 依赖（保留 UI 结构——四个字段 base/per100Chars/min/max 表单）

### 3. RepositoriesPage 注册表单 + 测试连接
- 接入表单（当前占位"仓库注册能力即将上线"）→ 接 REST：POST /api/v1/repositories（registerRepository——看 api/repositories.ts 是否已有——P1-4a 注册端点已实现但前端可能没接）→ 成功 toast + invalidate 列表 + 关闭表单
- 仓库详情/列表加"测试连接"按钮 → testRepository(id) → 结果 toast（ok → "连接正常 Xms"；fail → message）
- 移除占位提示

### 4. 验证
- npm run build + npm test（App.test 若挂 → vi.mock queries/config + queries/repositories 扩展 testRepository——参照现有 repositories mock）
- 浏览器实测（dev 4173 + 3003）：
  1. /settings 运行与预算 tab → 真实配置值（version 0 + 默认值）→ 修改 base → 保存 → toast + 值生效（GET 验证）
  2. /repositories 注册表单 → 填真实仓库（name/url/localPath 看表单字段）→ 注册 → 列表 +1 → **清理测试仓库**（DB 删除）
  3. 测试连接按钮 → 真实仓库 → "连接正常"
- git commit + push

## 不做
- backups 前端 UI（备份页后续）
- 其他 SettingsPage tab（模块开关 mock 保留——P2-4 处理或标注）
- 409 冲突 UI 的高级版（冲突合并——MVP 重拉即可）

## 审查（流程铁律）
完成后调用 codex-review-and-fix skill 审查（手动模式）；报告写 codex-review-p2-3b.md；修复后重新 build+test。

**开工前先用 mcp__codegraph__codegraph_explore 探索**（查询 "SettingsPage defaultConfig config savedConfig 运行与预算; RepositoriesPage 注册表单 接入表单; api/repositories registerRepository" 等，项目根 C:\Users\13588\Desktop\AI_Coding_Center）。
