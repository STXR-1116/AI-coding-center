# 微任务 P0-3：前端 API 层 + 登录 + auth/me 接入（AI_Coding_Center）

背景：本工程（C:\Users\13588\Desktop\AI_Coding_Center）是 CodingCenter 前端原型（100% mock）。后端 REST 已在主工程（http://127.0.0.1:3003/api/v1）实现 auth 四端点（P0-2）。本任务：前端建 API 层、登录页、auth/me 接入、React Query 首次启用。

后端实际形态（重要）：**鉴权走 session cookie（NextAuth），非 Bearer**——前端 dev 用 vite proxy 同源（cookie 自动带）。`POST /api/v1/auth/login` 收 `{ username, password }` 返回 `{ data: { user: { id, username, name, role }, capabilities: [] } }`；`GET /api/v1/auth/me` 返回 `{ data: { user, role, capabilities: [], visibleModules: [], defaultProject: null } }`（无 session → 401 `{ error: { code, message, requestId, retryable } }`）。**role 是大写枚举（EMPLOYEE/LEADER/PM）**——前端归一化。

## 范围（严格）

### 1. vite proxy + API client（src/api/client.ts 新文件）
- vite.config.ts 加 dev server proxy：`"/api": { target: "http://127.0.0.1:3003", changeOrigin: true }`
- `API_BASE = import.meta.env.VITE_API_BASE_URL ?? "/api/v1"`
- `ApiClientError extends Error`：`status/code/message/requestId/retryable`；`fromResponse(res, body)` 工厂
- `request<T>(path, opts?)`：`{ method, body?, idempotent? }`；POST 且 idempotent → 自动生成 `Idempotency-Key`（crypto.randomUUID）；带 `X-Request-Id`（randomUUID）；成功返回 `body.data`；非 2xx → 解析 error envelope 抛 ApiClientError（解析失败回退 INTERNAL_ERROR）
- 导出 `get/post/patch/put/del` 便捷函数

### 2. auth API（src/api/auth.ts 新文件）
- `login(username, password)` → POST /auth/login（idempotent）
- `fetchMe()` → GET /auth/me（401 时返回 null 或抛——由调用方决定；推荐返回 null）
- `logout()` → POST /auth/logout

### 3. 登录页（src/pages/LoginPage.tsx 新文件）
- 独立居中卡片页（品牌标记 + 用户名/密码输入 + 登录按钮 + 错误提示），风格对齐现有设计 token（styles.css）
- 提交 → login() → 成功：更新全局 auth state + 跳 `/tasks`；失败：显示 ApiClientError.message

### 4. 路由 + 守卫（src/App.tsx）
- `/login` 路由（无 ModuleGate）
- 新增 `RequireAuth` 包装：auth 未加载（me query pending）→ 加载态；未登录 → `<Navigate to="/login" />`；已登录 → children。业务 9 路由全部包 RequireAuth（/login 除外）
- 现有 user 硬编码（AppContext 里 Brandon/leader）替换为 me 返回的真实 user

### 5. AppContext 扩展（src/state/AppContext.tsx）
- auth state：`{ user, capabilities: string[], visibleModules: string[], status: 'loading'|'authenticated'|'anonymous' }`
- 初始化：useEffect 里 fetchMe() → 设 status；`login(username, password)` / `logout()` actions
- **归一化**：me 返回 role 大写 → 存小写（Role 类型 'employee'|'leader'|'pm'）——映射 EMPLOYEE→employee/LEADER→leader/PM→pm
- user 形状：`{ id, username, name, role }`（title 字段前端可留空或派生）

### 6. AppShell 真实用户（src/components/AppShell.tsx）
- 用户卡片（底部）：显示 `auth.user.name` + role 徽章（原 Brandon/产品经理 硬编码替换）
- capabilities 驱动导航（P0-4 细化——本任务仅：visibleModules 已由后端提供，AppShell 的 enabledModules 优先用 auth.visibleModules，无则回退现状）

### 7. 测试（src/App.test.tsx 补 + 新 tests）
- client：mock global.fetch → 成功返回 data；error envelope → ApiClientError（code/message）；非 JSON 回退
- auth 归一化：role 映射函数（抽 `normalizeRole(role: string): Role` 导出，单测 EMPLOYEE→employee 等）
- 现有 Vitest 全绿（npm test）

## 不做
- 不做 P0-4 按钮级 capabilities 权限改造（下个任务）
- 不迁移业务数据到 React Query（P1 起）
- 不改后端

## 验收
1. `npm run build`（tsc + vite build）通过 + `npm test` 全绿（含既有）
2. dev 实测：`pnpm dev`（4173）→ 访问 /tasks 未登录 → 跳 /login → leader/password123 登录 → 回 /tasks 显示真实用户 "Team Leader"+leader 徽章；刷新后保持登录（cookie）
3. git 初始化/提交（若前端工程无 git 则 git init + 首次提交；有则正常提交）
